//! ramsleuth-client — Shared IPC client for the ramsleuth daemon (Phase 3).
//!
//! Per the v2 roadmap this crate is a **library** (`src/lib.rs`, P3-18)
//! consumed by the CLI bin (P3-21 rewrites `main` with the `dump` /
//! `bench` / `status` subcommands) and later by the TUI/GUI frontends
//! (P3-22+ / P3-25+). This entry is still a scaffold stub: no behavior.

fn main() {
    // P3-21: subcommand dispatch lands here (dump / bench / status).
}
