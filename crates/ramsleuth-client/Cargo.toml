[package]
name = "ramsleuth-client"
description = "Shared IPC client for the ramsleuth daemon Unix socket"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
# Phase 3: add bincode/tokio and workspace dep on ramsleuth-telemetry.
# NOTE: the v2 roadmap treats client as a shared library; a [lib] target is
# added when TUI/GUI begin consuming it (see plans/PLAN.md, Phase 3).
