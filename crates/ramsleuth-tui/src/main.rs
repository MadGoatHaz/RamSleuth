//! ramsleuth-tui — Terminal user interface (Phase 4, ratatui + crossterm).
//!
//! Renders the dense timing matrix and the AIDA64-style benchmark grid in the
//! terminal (ideal for headless servers / SSH). Keybindings: `[R]` Run,
//! `[S]` Export snapshot, `[Q]` Quit.

fn main() {
    // Scaffold stub: no behavior yet.
}
