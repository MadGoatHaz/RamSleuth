# Build artifacts / caches
/target
**/*.rs.bk

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store

# Local working docs (cycle plans, checklists, research notes, handover) — stay local
Docs/
plans/

# Agent state (carries a full origin/master checkout) — never committable
.kilo/

# Development log files (lease board + durable cycle history) — not for end users, local-only
MASTER_LOG.md
DEV_LOG.md
