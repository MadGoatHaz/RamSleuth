# Build artifacts / caches
/target
**/*.rs.bk

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store

# Development documentation (cycle plans, research notes, handovers, AUR ops reference) — local dev-doc storage, NOT repo-facing
Work/
plans/

# Agent state (carries a full origin/master checkout) — never committable
.kilo/

# Development log files (lease board + durable cycle history) — not for end users, local-only
MASTER_LOG.md
DEV_LOG.md
