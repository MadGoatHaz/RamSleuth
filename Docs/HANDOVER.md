# RamSleuth v2 — Development Handover

> **Audience:** the next development cycle (**Cycle 10 — operator live sign-off of the Cycle 9 changes, the operator-gated push, and the remaining operator/hardware-gated items**). Self-contained: read this first, then `FULLSCOPEvsCOMPLETED.md` (full scope vs. completed, with the mermaid map + the GUI gap list), the other `Docs/` files, and `plans/`.
> **Date:** 2026-09-17 (Cycle 9 close-out). **Branch:** `v2-development` — local tip = **`c2f40a5`** (the final C9 lease sign-out, after the C9-07 merge), **UNPUSHED (operator-gated)** — **202 commits ahead of `origin/v2-development` @ `b908f7b`** (everything up to `b908f7b` is on origin; all 202 are local-only — see §8).
> **Sources of truth:** this file + `FULLSCOPEvsCOMPLETED.md` (full scope vs. completed, with the mermaid map) + `Docs/RamSleuth-v2.md` (roadmap) + `Docs/Grand Design & Architecture Specification.md` (spec) + `plans/` (per-phase plans — **Phases 1–7 all done**; the Cycle 6/7/8 plans are tracked at HEAD; the Cycle 9 plan + operator sign-off record = `plans/PLAN-CYCLE9.md` / `plans/CYCLE9-LIVE-CHECKLIST.md` — **untracked in the working tree**, commit at the Cycle 9 compaction) + `MASTER_LOG.md` (durable per-cycle history — Cycles 1–8 committed at HEAD (the C8 compaction is `6cc9840`); the Cycle 9 section is present in the working tree, uncommitted) + `DEV_LOG.md` (lease board — no active leases; the Cycle 9 per-lease record is in the working tree, uncommitted).
>
> **Full scope reference:** for the full project scope vs. completed breakdown (with mermaid map + the GUI gap section), see `FULLSCOPEvsCOMPLETED.md` at the workspace root.

---

## 1. Project Overview

RamSleuth v2 is a dual-layer hardware introspection + benchmarking utility for Linux x86_64:

1. **Live memory-controller telemetry** — active trained subtimings, clocks (MCLK / UCLK / FCLK, gear/div modes, GDM/PDM, command rate), CAD-bus drive strengths & terminations, and voltages (AMD via the `ryzen_smu` SMU PM table + the SMN registers; Intel via MCHBAR MMIO), plus SPD EEPROM decode (JEP106 module/die makers, rank, XMP 2.0/3.0, EXPO).
2. **AIDA64-style benchmark engine** — multi-threaded AVX2/AVX-512 non-temporal SIMD bandwidth (Read / Write / Copy × Memory / L3 / L2 / L1) and pointer-chase latency.

End goal: unprivileged TUI (ratatui) and GUI (egui/eframe) frontends served by a privileged daemon. **State of the frontends:** the TUI is complete (Cycle 3); the GUI is **complete (Cycles 6–7)** — the full Grand Design §3.1/§3.2 dashboard **plus the v2.0.0 polish**: the 3-line header (now unit-aware), Zone 1 compact 3-column with explicit `GEAR_DOWN`/`CR` labels + vendor-conditional timing blocks, Zone 2 flat status line + **burn-in mode** with live per-iteration cells, Zone 3 SPD cards with per-generation part numbers, and a **dedicated Graphs window** (native second OS viewport: 5 series, hover crosshair, horizontal pan, 1/5/15/60-min window) — the embedded ~10-minute trend strip is **removed**; polling is **baseline-once** (auto-refresh OFF by default). **All 8 GUI gap items are closed** (Cycle 6) **and the 7 v2.0.0 polish areas are closed** (Cycle 7). Packaging & distribution — **delivered in Cycle 4 (Phase 5)**. Live AMD verification — **closed in Cycle 5 (Phase 6)** with the ground-truth cross-check **VERDICT PASS** (§2, §3). The backend (daemon/CLI/telemetry/bench) is complete and live-verified; the remaining work is the operator live-run sign-off + the push + the operator/hardware-gated items (§5).

**Mandate:** 100% pure Rust, Cargo workspace (Edition 2021, resolver 2), Linux x86_64. No C++/Python/JS components.

## 2. Current State (2026-09-17 — Cycle 9 close-out, ready for Cycle 10)

- **Phases 1–6: ALL COMPLETE** (Cycles 1–5; Phase 6 = live-hardware verification, 8 chunks merged, O1 **VERDICT PASS** — §3). **Phase 7 (Cycle 6 — GUI workstream): COMPLETE** — all 8 GUI gap items closed (29 chunks; §3). **v2.0.0 polish (Cycle 7): COMPLETE** — the operator's 7-area Cycle 6 live-run feedback closed across **22 chunks (C7-01…C7-22, all `--no-ff` merged into `v2-development`)** (plan `plans/PLAN-CYCLE7.md`). **v2.0.0 header truth (Cycle 8): COMPLETE** — the operator's 4 header tasks closed across **11 chunks (C8-01…C8-11, all `--no-ff` merged into `v2-development`)** (plan `plans/PLAN-CYCLE8.md`; full per-chunk record in the in-flight `MASTER_LOG.md` Cycle 8 section + the git log). **v2.0.0 RAM topology / Graphs lifecycle / CPU-temp hwmon / right-column layout (Cycle 9): COMPLETE** — the 4 operator tasks closed across **8 chunks (C9-01…C9-08, all `--no-ff` merged into `v2-development`)** (plan `plans/PLAN-CYCLE9.md`; full per-chunk record in the `MASTER_LOG.md` Cycle 9 section). Rust delta since the Cycle 8 close (`5730b33..c2f40a5`): 24 commits (the C8 compaction `6cc9840` + the 8 C9 chunks; **no wire-shape change this cycle** — all GUI-local).
- **The GUI is now (after the Cycle 8 header-truth pass):** the **3-line header** — line 2 ends with the **provenance fragment**: `AGESA <v>` when a true AGESA token resolves from the DMI BIOS string, else `SMU <v>` (the `ryzen_smu` driver version — on this host: `… (BIOS: 5601, SMU 56.78.0)`), else `AGESA N/A` — **never `AGESA 56.78.0`** (the reported mislabel fixed; C8-11, D-1); the RAM line reads **`RAM: 62.68 GiB (2x16 GiB) 3200 MT/s | Dual-Channel | …`** — the `MemTotal`-preferred OS total (the 64 GB host minus reserved) + the corrected per-slot 16 GiB decode (C8-02/03/04, D-2/D-3 — the reported `4 GiB (2x2 GiB)` is gone); **Zone 1** — the redundant AMD `gear` row is **removed** (it read `N/A (not applicable)` on every AMD host) and the combined `GDM / CR` cell is **split into separate `GEAR_DOWN` and `CR` rows** (`Enabled` / `1T` on this host; Intel keeps its `gear` row; C8-10, D-6); every N/A in zones 1–3 + the Graphs window renders as **bare `N/A` in the muted `NA_GRAY` (0x8A8A94)** — the verbose `(<reason>)` parentheticals are stripped (the reason stays on the wire) and the N/A red is gone, while **fault-red is preserved** (the daemon-disconnected line + the `! <error>` line stay `CRIMSON`; the AMBER warnings — 1:2 desync, VDDCR_SOC > 1.30 V — untouched; C8-05…C8-09, D-4/D-5). The rest of the dashboard is the Cycle 7 state: **baseline-once polling** (auto-refresh OFF by default), the 3-column compact Zone 1 (no vertical scroll at 1400×900), Zone 2 flat status line + **burn-in mode** with live per-iteration cells, Zone 3 SPD cards with per-generation part numbers, the **dedicated Graphs window** (second native viewport), F2/F3/Q + the settings panel.
- **QA baseline (re-verified at Cycle 9 close on `c2f40a5`):** **541/541 whole-workspace tests green in debug AND release** (up from 527 at the Cycle 8 close; +14 across the C9 chunks); `cargo clippy --workspace --all-targets -- -D warnings` → **zero warnings**; **MSRV 1.75 declared** (`rust-version = "1.75"` unchanged; **zero new dependencies** — the whole cycle is `std` + the pinned egui/eframe 0.27.2 surface); **6 release binaries** build (bench / client / daemon / gui / telemetry / tui); **`Cargo.toml`/`Cargo.lock` diff over `5730b33..c2f40a5` = none** — **zero wire changes this cycle** (all 8 C9 chunks are GUI-local — no protocol / `messages.rs` shape delta; per-merge frozen-shape audits clean).
- **QA verdict: PASS-WITH-MANUAL-LIVE-VERIFY** — the automated QA audit (regression, clippy, build, MSRV, the frozen-shape audit, no-panic across every privilege × CPU state) passed fully on `c2f40a5` (541/541); the live GUI run needs interactive sudo (the daemon reads telemetry as root) + an operator-attended display on the 5950X host, so **the live checks are deferred to the operator** (`plans/CYCLE9-LIVE-CHECKLIST.md` for the Cycle 9 checks; the Cycle 8 checks remain §5.1).
- **Git state:** `v2-development` @ `c2f40a5`, **202 commits ahead of `origin/v2-development` @ `b908f7b`** (local-only, unpushed — operator-gated; §8). Working tree: **no code** — `DEV_LOG.md` modified (the Cycle 9 lease record — base line `c2f40a5`, ACTIVE_WORKERS cleared, per-lease history archived), `MASTER_LOG.md` modified (the Cycle 9 section appended at the top), `plans/PLAN-CYCLE9.md` + `plans/CYCLE9-LIVE-CHECKLIST.md` untracked (the Cycle 9 plan + the operator sign-off record), + this handover refresh — all uncommitted (the Cycle 9 compaction commits them; no push — §5.2).
- **Telemetry on the reference host after Cycle 8 (live confirmation = the operator run, §5.1):** the AMD section is unchanged — clocks 1800.00 MHz OneToOne, VDDCR_SOC 1.1375 V, **27 DRAM timings + GDM live (SMN)**, CAD/RTT/drive honest `N/A` (driver-side confirmation pending); `Intel: N/A (UnsupportedHardware)`; but the Cycle 8 changes land in the values — the platform line now carries **`smu_version` `56.78.0`** under its true label (the `agesa` field narrows to the DMI BIOS-string scan → `Na` on this host), the total capacity flips to the **`MemTotal` 62.68 GiB**, and the per-slot sizes decode **16 GiB each** (the byte-`0x02` DDR4 classification + the total-device decode — the reported `4 GiB (2x2 GiB)` and the live part-number `Na` are both fixed; the part now reads `F4-3600C18-32GVK` from `0x149`). No panic in any privilege/CPU state.
- **Verification CLIs (all work today, exit 0):**
  - `target/release/ramsleuth-daemon` — the single privileged process (root; or `--socket /tmp/ramsleuth.sock` for an unprivileged dev socket — the SOFT caps probe warns and keeps serving).
  - `target/release/ramsleuth-client -- dump | status | bench [--tier memory|l1|l2|l3|full] [--mode full|memory-only]` — **no sudo needed**; `dump` is the exit-criterion command (AMD: clocks + VDDCR_SOC + 27 timings + GDM live on this host); no daemon running → exit 1 + friendly `DaemonDown` hint (never a crash). (The CLI gains no burn-in subcommand this cycle — the GUI is burn-in's consumer; documented follow-up.)
  - `target/release/ramsleuth-tui` — terminal dashboard (R = refresh, S = snapshot, Q = quit; background 2 s updater).
  - `target/release/ramsleuth-gui` — the full §3.1/§3.2 desktop dashboard (**refresh OFF by default — one baseline fetch on connect**, the Graphs button spawns the second-window trend view, F2 = 640×420 validation-card PNG, F3 = JSON + bench export to `$HOME`, keyboard F2/F3/Q, the Settings panel, the dynamic socket; **no sudo needed** — Cycles 6–7, §3).
  - `sudo scripts/amd-ground-truth.sh` — **the Cycle 5 ground-truth cross-check** (needs root + the daemon up): sequential capture (`monitor_cpu -f` PM frame → `ramsleuth-client -- dump` → one-shot `monitor_cpu -m`), gates ±1 MHz / ±10 mV / ±1 tick, the 0x50200 set-point record, the GDM three-source cross-check; exit 0 = in tolerance (**VERDICT PASS — the 2026-09-14/15 run**), 1 = out of tolerance, 2 = preconditions unmet.
  - `sudo monitor_cpu` — the ryzen_smu ground-truth CLI (`/usr/bin/monitor_cpu`, mode 700 root; §6). **Note (Cycle 5 finding #2):** its PM-frame path is version-gated on 0x240903 and exits with ZERO frames unless `-f` is passed — this host's table is 0x380805, so use `monitor_cpu -f` (infinite frame loop; the `timeout` rc=124 kill is the normal capture path) or `-m` (SMN one-shot, exits on its own).
  - Direct verification CLIs (no daemon; from the Cycle 1/2 lineage, still valid): `cargo run -p ramsleuth-bench` — full 4×4 grid (text + hand-rolled JSON); optional `--avx512` (falls back to AVX2 when the host lacks AVX-512F — as on this host) and `--json` flags; unknown flag → exit 2. And `cargo run -p ramsleuth-telemetry` — dashboard-style telemetry listing; every cell renders as a value or `N/A (<reason>)`; optional `--json`; **exit 0 even when all sections are `Na`** (a structured N/A is a valid outcome). Run as root for live AMD data (the module is installed — §6).

## 3. Cycle summaries (Cycle 9: v2.0.0 RAM topology / Graphs lifecycle / CPU-temp hwmon / right-column layout — Cycle 5: live-hardware verification — Cycle 6: GUI workstream — Cycle 7: v2.0.0 polish — Cycle 8: v2.0.0 header truth)

### Cycle 9 (v2.0.0 RAM topology header / Graphs telemetry lifecycle / CPU-temp hwmon / right-column layout)

What was built: the **4 operator tasks on the 5950X host** closed across **8 chunks (C9-01…C9-08, all `--no-ff` merged into `v2-development`)** from baseline `6cc9840` (the Cycle 8 compaction) to tip `c2f40a5` (plan `plans/PLAN-CYCLE9.md`; full per-chunk record in the `MASTER_LOG.md` Cycle 9 section):

- **Task 1 — RAM topology header (C9-01, D-1):** the header RAM line now renders the topology grouped by (size, rank word) — **`2x16 GiB Single-Rank`** (a Na/0 rank omits the word) — plus a **slot note** (`2 of 4 slots SPD-visible`) when `MemTotal` exceeds the SPD sum. **GUI-local only** — `SpdModule.rank` was already wire-borne (C8-03); no wire change.
- **Task 2 — Graphs window telemetry lifecycle (C9-02, C9-03, D-2):** opening the Graphs window **force-enables the auto-refresh** (saving the original), and closing **reverts to the saved original** — one transition detector covers both close paths (the button toggle + the WM `close_requested`); the **in-window `Poll` combo** (500/1000/2000/5000/10000 ms presets; a non-preset stored value shows its exact ms) writes the shared `settings.poll_interval_ms`, which the poller re-reads live (the C9-03 `&mut u64` co-land — the child frame flips the read→write lock, the D6 settings-write precedent).
- **Task 3 — CPU temperature graphing (C9-04, D-3):** `read_cpu_temp_c` is now an **ordered scan** — the **hwmon source first** (each `hwmon*` `name` matched case-insensitive + trimmed, **`k10temp` > `zenpower`**, reading `temp1_input` ÷1000 — **never a fixed `hwmonN`**), then the pre-existing **`cpu_thermal` zone scan as fallback**; first finite wins, every failure class → NaN. On the live host (`hwmon4 = k10temp`) the CPU TEMP row now **plots instead of `N/A`**.
- **Task 4 — main-window layout uniformity (C9-05, C9-06, C9-07, C9-08, D-4):** the left-column frames (**bench** C9-06, **telemetry** C9-08) gain **`set_min_width(available_width)`** so their borders fill the column; the **right column is vertically split** — bench at natural height, **status** at the remaining-height slice inside the retained ScrollArea overflow fallback (C9-05) — and the status frame fills the **full width AND remaining height** (`set_min_width` + `set_min_height`, C9-07).

Plan decisions (one line each; full record in `plans/PLAN-CYCLE9.md`):
- **D-1** — the rank word + slot note are **GUI-local** (`SpdModule.rank` was already wire-borne, C8-03 — no wire change).
- **D-2** — **force-enable the refresh on Graphs open, revert on close** (one transition detector covers both close paths); the **in-window `Poll` combo** writes `settings.poll_interval_ms` (persists to Settings, not a Graphs-local override).
- **D-3** — `read_hwmon_temp_c` **name-matches `k10temp` first, else `zenpower`** (never a fixed `hwmonN`), reading `temp1_input` ÷1000; the **`cpu_thermal` zone scan is the fallback**.
- **D-4** — **`set_min_width(available_width)`** in the bench / status / telemetry zone frames + **`set_min_height`** in the status zone + the **right-column vertical split** in `main.rs`.

Merge commits (all `--no-ff` into `v2-development`; per-chunk record in the `MASTER_LOG.md` Cycle 9 section): **C9-01** RAM header `66c7246` · **C9-02** Graphs lifecycle `1e20bfd` · **C9-03** poll-control co-land `985248f` · **C9-04** CPU-temp hwmon `02af5f2` · **C9-05** right-column split `502c7b7` · **C9-06** bench width `b88784f` · **C9-07** status fill `c2f40a5` · **C9-08** telemetry width `3899f21`.

QA: **541/541 tests green in debug AND release** (up from 527 at the Cycle 8 close; +14), `cargo clippy --workspace --all-targets -- -D warnings` → **zero warnings**, **MSRV 1.75** held, **zero new dependencies**, **6 release binaries**, **zero wire changes** (all 8 chunks GUI-local — no protocol / `messages.rs` shape delta; per-merge frozen-shape audits clean). **Verdict: PASS-WITH-MANUAL-LIVE-VERIFY** — the operator live GUI run is the remaining manual gate (`plans/CYCLE9-LIVE-CHECKLIST.md`).

Push state: `v2-development` @ **`c2f40a5`** (Cycle 9 range `6cc9840..c2f40a5`; `origin/v2-development` still `b908f7b`), **202 commits ahead** — **unpushed (operator-gated)**; the 8 remote `branch/chunk-c9-*` chunk branches are the handover prune target (all fully merged; on go-ahead: fast-forward to `c2f40a5`, NEVER force-push, prune, optional `v2.0.0` tag).

Carried follow-ups (still open): the TUI/CLI N/A twins restyle · the optional root-mode AGESA source · broader vendor hwmon names (Intel `coretemp`) · the G.Skill SPD hub spec-annex audit · the `die_type` label map · settings XDG persistence · the `q`-focus Quit guard · a shared `rank_word` helper in a common module.

### Cycle 5 (Phase 6 — Live-Hardware Verification & Remaining Reconciliation)

What was built: **8 chunks merged** (all single-file, all `--no-ff` into `v2-development`; full per-chunk history in `MASTER_LOG.md`):

- **P6-01 `scripts/amd-ground-truth.sh`** (`2a1ead2`) — the matched-condition cross-check: preconditions (root / `monitor_cpu` / reachable daemon / driver attrs → exit 2 + actionable), sequential capture, gates PM clocks ±1 MHz + VDDCR_SOC ±10 mV + the 27 timings ±1 tick (active only when the dump cell holds a value; N/A cells deferred), the 1792-vs-1800 investigation record (the 0x50200 set-point + both MCLK sources + the PM f32), exit 0/1/2.
- **P6-02 `crates/ramsleuth-telemetry/src/amd_smn.rs`** (`9c1cdf2`) — the SMN sysfs accessor (canonical `ryzen_smu_drv` kobject first; the libsmu write-address→read-value protocol; `FdGuard` RAII; error classification via the existing `classify_io_error`) + the verified 13-register bitfield table (19 in-file tests; a documented single-file exception, the P5-15 precedent).
- **P6-03 `crates/ramsleuth-telemetry/src/facade.rs`** (`d76e411`) — the AMD branch is now `acquire → parse → apply_smn → map`; the overlay is infallible (DriverMissing = whole no-op; per-register containment); PM fields never touched; 4 wiring tests.
- **P6-04 `crates/ramsleuth-telemetry/src/spd_decode.rs`** (`982c948`) — density `0x0D` → 16 Gb (checked before the published family so it cannot shadow it) + maker `0xC1` → G.Skill (JEP106); the live `F4-3600C18-32GVK` image fixture + 4 regression tests.
- **P6-05 `crates/ramsleuth-bench/src/worker.rs`** (`36d1851`) — sub-4 MiB inner-loop iterations (table-pinned; large tiers byte-identical; checksum conventions frozen; `run_pinned` signature + wire shapes unchanged; 3 tests).
- **P6-09 script `gtfix`** (`667e11d`, retroactive — operator live run #1) — `monitor_cpu -f` PM-frame capture (the host's 0x380805 table gets ZERO frames without it), LC_ALL=C box-drawing parse + exact label match ("Fabric Clock" must not catch "Fabric Clock (Average)"), the 0x50200 two-stage protocol replicated verbatim from `monitor_cpu.c` with the **corrected LE address bytes `00 02 05 00`** (the P6-01 draft wrote `00 02 50 00` = 0x500200 byte-swapped — the origin of the live sentinel), `0xffffffff` → READ-FAILED (never the 4233-MHz misread) + GDM UNKNOWN + the three-source cross-check, CAD message → pending driver-side confirmation.
- **P6-10 daemon sentinel** (`5817e28`, retroactive) — the driver leaves `0xffffffff` in `smn_result` on a failed PCI read (userspace `read()` still succeeds): a sentinel probe arm + `read_word` Err/sentinel→None + decode-level `word_at` normalization — the coincidental bit-11-of-all-ones GDM=1 is gone; per-register containment; a sentinel probe keeps the PM-table parse value; 4 tests.
- **P6-11 script `gttimeout`** (`51f4c4f`, retroactive — operator live run #3) — with `-f`, `monitor_cpu` runs an infinite frame loop that `timeout 2` always SIGTERM-kills — **rc=124 is the normal capture path** and is now accepted (zero-rows-with-kill = explicit "produced nothing" failure; every other non-zero rc unchanged; multi-frame parse = last-occurrence-wins).
- **Parked (never stalled the pipeline):** P6-06 (Intel live decode — hardware-gated, the i5-6600 not yet attached), P6-07 (MSRV decision record — operator-gated, not authored), P6-08 (push runbook — operator-gated, not authored; the runbook content is §8).

**Live verification (the cycle's exit criterion):** four operator live re-runs of the ground-truth script drove the three retroactive fixes (P6-09/10/11); the final run (post P6-11) returned **VERDICT: PASS — 31 active gates in tolerance** (clocks ±1 MHz; VDDCR_SOC ±10 mV; 27/27 timings tick-identical, 0 deferred; set-point 1800 MHz; GDM triple-confirmed; CAD honest N/A).

**QA:** 371/371 debug + release (337 → +19 P6-02 → +4 P6-03 → +4 P6-04 → +3 P6-05 → +4 P6-10; P6-09/11 script-only, no Rust delta), `clippy -D warnings` clean, MSRV 1.75 held, `Cargo.lock` untouched, zero panics/segfaults in any privilege × CPU state. Script gates: `bash -n` + shellcheck 0.11.0 zero findings (317 lines, mode 755); the stub harness (19 executions / 75 assertions, all green) validated the script logic headlessly (the uncommitted harness lived in a retained worktree — `.tests/gt-stub/` — for future reuse).

### Key findings recorded (Cycle 5)

1. **The 5950X host runs a G.Skill F4-3600C18-32GVK kit = DDR4-3600** (1800 MHz MCLK set-point) — the kickoff's "DDR4-3200" was nominal/wrong (the SPD's 3200 MT/s *base* speed misled it); the earlier "1792 vs 1800" delta was **transient retraining, not a scaling bug**.
2. **`monitor_cpu` hard-gates its PM-frame path on PM-table version 0x240903** and exits with ZERO frames unless `-f` is passed — this host's table is 0x380805, so the capture must use `monitor_cpu -f` (infinite frame loop → the `timeout` rc=124 kill is the normal capture path).
3. **The ryzen_smu driver leaves `0xffffffff` in `smn_result` on a failed PCI read** (the userspace `read()` still "succeeds") — it is a failure sentinel, never data. Register **0x50200 specifically fails at the PCI level on this firmware (56.78.0)** while 0x50204–0x50264 read fine. The daemon previously decoded bit 11 of the all-ones word as GDM=1 **by coincidence** (P6-10: sentinel → Na; the PM-table parse value survives). The script's set-point falls back to the PM f32 @0x0CC, which is authoritative.
4. **SPD:** density `0x0D` = 16 Gb (vendor/legacy encoding outside the published `0x10..=0x17` family); maker `0xC1` = G.Skill (JEP106) (P6-04).
5. **The tRP mask is 6 bits at bit 16 (21:16)** per the driver reference — the original plan table's 22:16 was a transcription error (the reference wins).
6. **The P6-01 draft had the 0x50200 address bytes swapped** (`00 02 50 00` = 0x500200); the correct LE bytes are `00 02 05 00` — the wrong register is the origin of the live sentinel (P6-09).

### Cycle 6 (Phase 7 — GUI workstream)

What was built: **all 8 GUI gap items closed** (against `FULLSCOPEvsCOMPLETED.md` §"GUI Gap") — 29 chunks (C6-01…C6-30, C6-08 retired) in two waves, all `--no-ff` merged into `v2-development`:

- **The data-model / wire wave (C6-01…C6-19, interface freeze — the sanctioned frozen-shape changes, D-C10/D-C11):** the new `SystemPlatform` struct (`cpu_clock_mhz` / `motherboard` / `bios` / `agesa` — DMI + `/proc`-sourced, N/A-safe) + `mem_total_gib`; `SystemMemoryTelemetry` += `platform` / `total_capacity` / `dimm_sizes` (per-DIMM = density × devices / 8192; total = Σ dimm_sizes, else `/proc/meminfo`); `SpdModule` += `die_maker` / `die_type` / `devices`; the `CommandRate` enum + `ClockReadout.command_rate` (raw 0 → 1T, 1 → 2T, else `Na`; Intel honest `Na(NotApplicable)` — no speculative mapping, D-C11); the `AmdPmSnapshot.command_rate` raw slot + `SmnFields.command_rate` (0x50200 bit 10 — `apply_smn` now surfaces gdm + timings + command_rate); the cross-crate test-fixture ripple (C6-09…C6-19: protocol / client / daemon / tui / gui fixtures) closed the red window the wire-shape changes introduced — the hard `cargo test --workspace` gate landed post-C6-19. All with the serde/bincode round-trip gates; no other frozen arm shapes touched.
- **The GUI wave (C6-20…C6-30, items 1–8):** (1) the 3-line header — platform tag; CPU + motherboard/BIOS/AGESA; RAM total + per-DIMM + channel mode + sync-mode summary; (2) Zone 1 regrouped into 2 sub-columns × 3 section-pairs; (3) the GDM / command-rate row; (4) Zone 2 per-cell live bench fill from the `BenchProgress` stream; (5) Zone 3 SPD cards (product line, DRAM die, human rank label); (6) history — a 300-sample ring buffer + hand-rolled immediate-mode sparklines (no new dep); (7) settings — `GuiSettings` + the dynamic clamped poll interval (100…60000 ms) + the refresh gate (in-memory; the poller re-reads the live knobs, no restart); (8) export parity — the F2 640×420 validation-card PNG (title/timestamp + CPU/RAM lines), the F3 JSON with a top-level `bench` key (the frozen wire root flattened via a Serialize-only wrapper, byte-compatible telemetry keys), the keyboard F2/F3/Q (fresh key-downs only, no repeat flood; routed through the same `handle_action` as the buttons), and the dynamic socket (the poller reads `settings.socket` live per cycle — a panel edit retargets the running poller, no restart; the CLI `--socket` seeds it).
- **Plan decisions (full record in `plans/PLAN-CYCLE6.md` + `plans/PLAN.md`):** D-C10 (the sanctioned test-fixture red window during the wire freeze), D-C11 (Intel `command_rate` honest `Na`), the refined circuit-breaker.

**QA:** 452/452 debug + release (371 → +81 across the C6 waves), `clippy -D warnings` clean, MSRV 1.75 held, zero new deps, `Cargo.lock` stable at the tip, 6 release binaries, zero panics in any privilege × CPU state. **Verdict: PASS-WITH-MANUAL-LIVE-VERIFY** — the operator live run (`plans/CYCLE6-LIVE-CHECKLIST.md`) was the remaining manual gate; its 7 feedback areas became the Cycle 7 scope (now closed — the checklist is retained in the repo, its run superseded by the Cycle 7 checks, §5.1).

**Follow-ups (resolution at Cycle 7 close):**

1. **"q"-focus Quit guard** — the raw event scan fires `Q` even while the settings socket `TextEdit` has focus (typing `q` while editing the socket quits) — **carried to Cycle 8** (§5.7).
2. **`SpdModule.die_type` label mapping** — the field is wired (C6-02) but the label mapping defaults to `Na` (the die *maker* is live, e.g. SK Hynix) — **carried to Cycle 8** (§5.7).
3. **Settings XDG persistence** — `GuiSettings` is in-memory only (per process launch; the CLI `--socket` seeds it) — **carried to Cycle 8** (§5.7).
4. **Zones Units/Theme formatters** — **CLOSED in Cycle 7** (C7-11/C7-15: the header lines + the Zone 1 clock rows now consume the capacity/clock unit knobs; the theme stays the single DarkSlate variant by design). The settings-combo **desync** reported alongside them is fixed in C7-10 (the egui widget-Id collision — Cycle 7 key finding 1 below).

### Cycle 7 (v2.0.0 polish: UI refactor, telemetry lifecycle, burn-in, graphs window)

What was built: the operator's **7-area Cycle 6 live-run feedback** closed across **22 chunks (C7-01…C7-22, all `--no-ff` merged into `v2-development`)** (plan `plans/PLAN-CYCLE7.md`; the decision records D-1…D-8 live in the plan):

- **Polling lifecycle (C7-08/C7-09, D-8):** auto-refresh now **defaults OFF** — a **baseline-once** mechanism: exactly one `poll_telemetry` per *distinct* `settings.socket` value (a thread-local `baseline_for`), **regardless of `refresh_enabled`**, stamping `last_poll` after each; continuous polling then requires `refresh_enabled && due` (the existing gate, unchanged). A failed baseline = the normal friendly disconnected state (no retry storm — the next socket change or a refresh-enable re-triggers); a socket edit re-triggers exactly one fetch (the settings panel is never a dead end); enabling refresh resumes the full cadence one interval after the last fetch.
- **Settings (C7-10):** the dropdown **desync fixed** — the three `ComboBox::from_label("")` knobs (capacity / clock / theme) all derived the same egui widget `Id` (`from_label` → `Id::new("")`), so they shared one dropdown memory and desynced each other; each now uses a **unique `from_id_source` id**. The socket field's min width fixed (the full `/run/ramsleuth/ramsleuth.sock` renders untruncated).
- **Header / platform (C7-01/C7-11/C7-14/C7-15, D-6):** the AGESA validator relaxed (group2 bound 2–6 → **1–6 digits**) so the existing `ryzen_smu` `version`-attribute fallback resolves **`56.78.0`** on this host (no DMI field carries it; the DMI scan stays primary — Cycle 7 key finding 2); the header lines + the Zone 1 clock rows now **consume the capacity/clock unit knobs** (GiB/TiB, MHz/GHz); **vendor-conditional timing blocks** — the unsupported-vendor N/A block is omitted entirely (no "Intel: N/A (unsupported hardware)" on this AMD host).
- **Zone 1 (C7-12/C7-13, D-7):** the 3×2 **compact 3-column layout** — all six sections at 1400×900 **without a vertical scrollbar**; the GDM/CR row reads explicit **`GEAR_DOWN: Enabled/Disabled · CR: 1T/2T`** labels (the "Gear 1" wording dropped — it collides with Intel gear-mode semantics; a not-applicable/failed token degrades its own `N/A (<reason>)`, the combined cell CRIMSON when either token is absent).
- **Zone 2 + burn-in (C7-05/C7-06/C7-07/C7-16/C7-17/C7-18, D-1/D-2):** the progress pill retired → a **flat status line** (`Status: Idle` dim / `Status: Running… <m:ss>` — burn-in elapsed from the newest tick, a normal bench from the zone-local start clock — / `Status: Done` green); the **burn-in engine** (`run_burn_in` in `streamed.rs`, sharing a behavior-identical `run_cell_pass` extracted from `run_streamed` — a duration deadline, per-iteration `BurnInTick` emits, `duration_minutes = 0` = infinite/cancel-only stop); the **burn-in wire freeze** (C7-07 — one atomic co-land: `Request += StartBurnIn { target, duration_minutes }`, `Response += BurnInProgress(BurnInTick)`, `JobEvent += BurnInTick`, daemon `start_burn_in` on the same single-flight slot/claim/cancel machinery, + every production match-site guard in client/TUI/GUI — the D-C10 precedent scaled; every existing wire arm byte-identical); the GUI burn-in path (minutes `DragValue` 0..=1440 with 0 = ∞ + a `Run Burn-In` button, all run buttons single-flight, Cancel stops a burn-in, the live per-iteration row + the 4×4 grid wired to `burn_in.latest`).
- **Zone 3 / SPD decode (C7-02/C7-03/C7-04):** **per-generation module part-number decode** — DDR3/DDR2 `0x81..0x91` (unchanged), **DDR4 `0x149..0x15D`**, **DDR5 `0x200..0x220`** — and **`XMP3_BASE` moved `0x200` → `0x300`** (JESD79-5 — the DDR5 part-number/profile collision fixed, part + profiles coexist); the cross-crate fixture ripple (C7-04). Decode-internal: the `SpdModule.part`/`serial`/`profiles` shapes are untouched.
- **Graphs window (C7-19/C7-20/C7-21/C7-22, D-3/D-4/D-5):** the embedded **TREND HISTORY strip removed** (the columns reclaim the full central-panel height; `TelemetryData.history` + `record_history_sample` + `history.rs` kept — the graph state reuses the RingBuffer); the **graph state** (a 5-series ring at 1800 samples = 60 min @ the 2 s poll; one sample per successful poll, Na-guarded — no all-NaN holes; the bandwidth series is a **step series** from `latest_memory_read_bw`, D-5 — not a continuous sampler; the CPU-temp source = an unprivileged `/sys/class/thermal` cpu-zone scan → NaN on this host, D-4); the **Graphs header button + the native multi-viewport spawn** (eframe 0.27.2 `show_viewport_deferred` + per-frame keep-alive re-registration + `ViewportInfo::close_requested()` — one child OS window sharing the `Arc<RwLock<…>>` as a **pure reader** (D6: the poller stays the only writer); the D-3b dedicated-thread fallback proven unnecessary — Cycle 7 key finding 4); the **interactivity layer** (hover crosshair + a timestamp tooltip with the 5 values, horizontal drag-pan clamped to the span, 1/5/15/60-min window buttons — session-local render state in the child context's `IdTypeMap`).

**QA:** 515/515 debug + release (452 → +63 across the C7 chunks), `clippy -D warnings` zero, MSRV 1.75 declared, **zero new dependencies**, `Cargo.lock` stable at the tip, 6 release binaries, the frozen-shape audit clean (the only wire changes = the C7-07 co-land; all three frontends + the daemon consume the payload), zero panics/segfaults in any privilege × CPU state (incl. a burn-in cancel + duration stop, and the Graphs window open/closed via both the button and the WM X while polling). **Verdict: PASS-WITH-MANUAL-LIVE-VERIFY** — the 5 deferred operator live checks are §5.1.

### Key findings recorded (Cycle 7)

1. **The settings dropdown desync was an egui widget-Id collision** — the three `ComboBox::from_label("")` knobs (capacity / clock / theme) all derived the same `Id::new("")` as their `id_source`, so they shared one dropdown memory and desynced each other (the operator-confirmed root cause of the Cycle 6 live-run feedback); fixed with **unique `from_id_source` ids** (C7-10).
2. **AGESA on this host comes from the `ryzen_smu` `version` attribute `56.78.0`** — no DMI field carries it (`bios_version` = `5601`, no dotted token), and the conservative validator rejected the smu fallback (group2 `"0"` = 1 digit against the old 2–6 bound) → the header's AGESA was `N/A`; relaxing the group2 bound to **1–6 digits** (D-6) lets the existing fallback chain resolve it with the DMI scan still primary (C7-01).
3. **The DDR4 module part number lives at `0x149`–`0x15C`** — the old `0x81` read was the **DDR3 location** (blank on DDR4 → the reported "part number N/A" defect); DDR5 part at `0x200`, and `XMP3_BASE` moved `0x200` → `0x300` per JESD79-5 (the DDR5 part-number/profile collision; C7-02/C7-03). DDR3/DDR2 `0x81..0x91` unchanged.
4. **eframe 0.27.2 native multi-viewport works for the second window** — `show_viewport_deferred` + per-frame keep-alive re-registration + `close_requested()` spawn a real child OS window on the host's Wayland session, sharing the one event loop / context / state (C7-21, D-3; the compile-side spike passed headless, the live Wayland run is §5.1) — the **D-3b dedicated-thread fallback was unnecessary**.
5. **VDDCR_CPU and CPU temperature have no source on this host** — no PM-table field in the unprivileged SMU surface and no CPU thermal zone (`/sys/class/thermal` is iwlwifi-only → the runtime scan returns NaN) — both graph rows render as honest **`N/A (no source)`** (D-4); a first-class `cpu_temp_c` wire field (the C6-style shape route) is a later-cycle option for CLI/TUI parity.

### Cycle 8 (v2.0.0 header truth: AGESA/SMU relabel, RAM capacity, N/A styling, GEAR_DOWN split)

What was built: the operator's **4 header tasks on the 5950X host** closed across **11 chunks (C8-01…C8-11, all `--no-ff` merged into `v2-development`)** (plan `plans/PLAN-CYCLE8.md`; full per-chunk record in the in-flight `MASTER_LOG.md` Cycle 8 section):

- **Task 1 — AGESA/SMU relabel by provenance (C8-01, C8-11, D-1):** the wire freeze appends the additive `SystemPlatform.smu_version: Section<String>` (shape-checked, co-landing every test literal — the C7-07 precedent scaled); `agesa` narrows to the true-AGESA DMI BIOS-string scan (→ `Na` on this host — no unprivileged AGESA source exists, key finding 1); the header line 2 renders the provenance fragment — `AGESA <v>` (a true AGESA token present, suppresses the SMU value) / **`SMU <v>` (this host: `SMU 56.78.0`)** / `AGESA N/A` — **never `AGESA 56.78.0`**, never a hybrid, never a fabricated string.
- **Task 2 — RAM capacity (C8-02, C8-03, C8-04, D-2/D-3):** the SPD memory-type classification re-anchored to **byte `0x02`** (`0x0C` + 512 B = DDR4; the legacy byte-`0x00` + image-length fallbacks preserved — the live host's `0x00 = 0x23` junk no longer misclassifies it, and the part decodes from the `0x149` primary); the total-device decode per JESD79-4/5 — `devices` = **total** (rank × per-rank, per-rank derived from the 64-bit bus width when the vendor hub nibble fails consistency — the G.Skill non-compliant hub worked around, never trusted; live: 1 rank × x8 → **16 GiB per slot**); `total_capacity` flips to **`MemTotal`-preferred** (the OS ground truth — 62.68 GiB on this 64 GB host) with the SPD sum demoted to the non-meminfo `sum_dimm_sizes` fallback → the RAM line reads **`RAM: 62.68 GiB (2x16 GiB) 3200 MT/s | Dual-Channel | …`**.
- **Task 3 — N/A formatting + styling (C8-05…C8-09, D-4/D-5):** the new muted **`NA_GRAY` (0x8A8A94)** palette const + re-export; every GUI N/A (zones 1/2/3 + the Graphs window no-source note) renders as **bare `N/A` in `NA_GRAY`** — the verbose `(<reason>)` parentheticals stripped (the reason stays on the wire for the CLI/TUI); the not-started bench arm goes gray (unavailable, not a fault); **fault-red preserved** — the daemon-disconnected line, the `! <error>` line, and the `Asynchronous 1:2` desync stay `CRIMSON`, all AMBER warnings untouched; the TUI/CLI N/A twins keep their verbose + red forms this cycle (documented follow-up, §5.7a).
- **Task 4 — gear-metric removal (C8-10, D-6):** the redundant **AMD `gear` row removed** (`gear_mode` is always `Na(NotApplicable)` on AMD — the row read `N/A (not applicable)` on every AMD host); the combined `GDM / CR` cell **split into separate `GEAR_DOWN` and `CR` rows** (bare `Enabled` / `1T` on this host; each `Na` degrades its own row to bare gray `N/A`); `readout_sections` gains the `gear_row: bool` vendor parameter (`false` at the AMD call site, `true` at the Intel ones — the `gear` row survives on Intel, where it decodes `1x`/`2x`/`4x`); row counts: AMD 7 → 7 (−`gear` +`CR`), Intel ch0 8 → 9 (the split).

**Plan decisions (one line each; full record in `plans/PLAN-CYCLE8.md` §4):**
- **D-1** — AGESA: honest relabel, not a fabricated string: `agesa` = the true-AGESA DMI BIOS-string scan only (→ `Na` here — the real AGESA token is root-gated); the SMU firmware version moves to the additive `smu_version` field; the header shows `AGESA <v>` / `SMU <v>` / `AGESA N/A` by provenance — one value, one true label.
- **D-2** — per-slot capacity: `SpdModule.devices` = **total** device count via a generation-scoped JESD79-4/5 decode with the bus-width consistency/derivation rule (the vendor hub worked around when non-compliant); classification moves to byte `0x02`, the legacy `0x00` + image-length fallbacks preserved.
- **D-3** — total capacity: **`MemTotal` preferred** (the OS ground truth; the SPD enumeration is structurally incomplete on this host — 2 bound of 4 DIMMs); the SPD sum demoted to the non-meminfo fallback; per-slot sizes stay SPD-derived (no scaling — that would fabricate a 4-DIMM config).
- **D-4** — N/A formatting: **bare `N/A`**, GUI-scoped (the reason stays on the wire as `NaReason`; the TUI/CLI twins keep their verbose forms — documented follow-up).
- **D-5** — N/A color: a new muted **`NA_GRAY` (0x8A8A94)** for unavailable data (N/A is *unavailable*, not a critical fault); **fault-red `CRIMSON` preserved** for genuine critical faults (daemon status / error line / 1:2 desync) + all AMBER warnings untouched.
- **D-6** — Zone 1: the **AMD `gear` row removed** (always `Na` on AMD); the combined `GDM / CR` row **split into `GEAR_DOWN` + `CR`** (the D-7 token names become the row labels, each value bare, each `Na` degrading its own row); `gear_row: bool` vendor parameter — Intel keeps its `gear` row.

**QA:** 527/527 debug + release (515 → +12 across the C8 chunks), `clippy -D warnings` zero, MSRV 1.75 held, zero new deps, `Cargo.toml`/`Cargo.lock` diff none, 6 release binaries, the frozen-shape audit clean (the only wire change = the additive `smu_version`), zero panics/segfaults in any privilege × CPU state (incl. a bench run mid-flight with not-started cells + the Graphs window open/closed). **Verdict: PASS-WITH-MANUAL-LIVE-VERIFY** — the 7 deferred operator live checks are `plans/CYCLE8-LIVE-CHECKLIST.md` (authored at close) = §5.1.

### Key findings recorded (Cycle 8)

1. **No real AGESA string is obtainable unprivileged (this host).** The real AGESA token (`ComboAm4v… PI x.y.z.n`) lives only in SMBIOS Type 11 / the raw DMI table — both `0400 root:root` (unprivileged `strings`/`dmidecode -t 11` → `Permission denied`) — and the unprivileged DMI sysfs `bios_version = "5601"` is a bare OEM build code (no dotted token, no `AGESA` keyword). The only AGESA-shaped string on the host is the `ryzen_smu` driver `version` attribute `56.78.0` — the **SMU firmware version**, not an AGESA release; it was the mislabeled `AGESA 56.78.0` the operator reported. The honest fix is the provenance relabel (D-1); a root-mode AGESA source is a follow-up, not this cycle.
2. **The live module's SPD memory type lives in byte `0x02`, not byte `0x00` (a latent decode bug fixed this cycle).** The old classifier read `0x00` bits 4:0 — junk `0x23` on this host → "not DDR4" → the part decode fell to the legacy `0x81` location (non-ASCII → `Na`) — the root cause of the Cycle 7 deferred live check "SPD part on the live host" failing (not the offset). JESD79-4/5: `0x02 = 0x0C` (+ 512 B) = DDR4, `0x02 = 0x0C` (+ 1024 B) = DDR5, `0x02 = 0x0B` = DDR3; byte `0x00` is `SPD bytes used`.
3. **The G.Skill F4-3600C18-32GVK SPD hub area is non-compliant.** `0x80 = 0x11` (per-rank nibble `1` — `1 × x8 ≠ 64`-bit bus), `0x82 = 0x41` / `0x83 = 0x01` undefined in the model, `0x00 = 0x23` junk — the vendor hand-wrote this area. D-2's bus-width consistency/derivation rule works around it (per-rank derived as `64 / 8 = 8` → 8 total devices → 16 GiB); a spec-annex audit of the DDR4 module-specific section (`0x80..0x83` exact field map) is a decode-quality follow-up (the live shape is pinned as the regression, so a tighter map only replaces the derivation).
4. **The SPD enumeration is structurally incomplete on this host — the total must prefer `MemTotal`.** Only 2 of the 4 DIMMs are `ee1004`-bound (`MemTotal` = 65728012 kB = 62.68 GiB; the other ~32 GiB sits in slots whose EEPROMs are not bound), so the SPD sum can never equal the OS total, and the old "sum when any cell is a `Value`" rule let the bogus 2 GiB cells block the meminfo fallback (the reported `RAM: 4 GiB`).

## 4. The AMD Telemetry Model (reconciled across Cycles 4–7)

**PM table (P5-15, Cycle 4):** the version is the `TableVersionId` in the sibling `/sys/kernel/ryzen_smu_drv/pm_table_version` file (4-byte LE `u32`), **not** in the blob; the blob is a headerless LE `f32` array, MIN_LEN 0x518 (326 × f32): `VDDCR_SOC` @ 0x0B0 (volts → mV ×1000), `FCLK` @ 0x0C0, `UCLK` @ 0x0C8, `MCLK` @ 0x0CC (MHz); `div_mode` derived (UCLK == MCLK → OneToOne). Accepted versions: Vermeer / Zen 3 (this host; live `0x380805`) + Matisse / Zen 2 sets. The legacy 7.11.x/12.x/13.x match, the u16-region layout, and the blob-header cross-check are gone. The char-dev `/dev/ryzen_smu` ioctl path (P2-03) is retained as 53XU-fork tolerance only.

**SMN registers (P6-02/03/10, Cycle 5; command-rate slot C6-05, Cycle 6) — the sanctioned channel for what is NOT in the PM table** (the 27 DRAM subtimings, GDM/PDM, command rate, CAD-bus drive strengths/terminations, VDDIO_MEM, VPP):

- **Access:** the driver's `smn` sysfs attr (`/sys/kernel/ryzen_smu_drv/smn`, RW) — a **write-address→read-value** protocol: write the 4-byte LE register address, read the 4-byte LE result, on one `O_RDWR` fd (the libsmu semantics; only the address word is ever written — never a register value). Canonical `ryzen_smu_drv` kobject first, legacy `ryzen_smu` second. **Never raw MMIO.**
- **The verified map (the `monitor_cpu` reference):** `0x50200` (MCLK set-point `(v & 0x7f)/3×100` MHz + **GDM = bit 11** + command rate bit 10 — the command rate is now **stored as `ClockReadout.command_rate`** (Cycle 6, C6-03/04/05: raw 0 → 1T, 1 → 2T, else `Na`; Intel honest `Na`)), `0x50204` (tCL / tRAS / tRCDRD / tRCDWR), `0x50208` (tRC / tRP — **tRP = 6 bits at bit 16 (21:16)**, not the plan table's 22:16), `0x5020C` (tRRDS / tRRDL / tRTP), `0x50210` (tFAW), `0x50214` (tCWL / tWTRS / tWTRL), `0x50218` (tWR), `0x50220` / `0x50224` (the tRDRD / tWRWR quadrant groups), `0x50228` (tWRRD / tRDWR), `0x50254` (tCKE — not stored), `0x50260` (tRFC1 / tRFC2 / tRFC4), `0x50264` (the tRFC mirror; sentinel `0x21060138`). **The +0x100000 two-stage rule:** when the 0x50200 probe reads marker `0x300`, every register (incl. the 0x50200 re-read) is read at base + 0x100000 (the offset is decided by the first read only).
- **The failure sentinel:** `0xffffffff` = a failed PCI/SMU read (the driver's `smn_result` is not updated; the userspace `read()` still succeeds) — it is **never decoded** (per-register containment; a sentinel probe keeps the PM-table parse value, which zeroes gdm/timings/command-rate). On this firmware (56.78.0) **0x50200 fails at the PCI level while 0x50204–0x50264 read fine**, so the set-point falls back to the PM f32 @0x0CC (authoritative) and GDM is confirmed by the three-source cross-check.
- **CAD/RTT/drive + PDM: confirm-or-Na** — their bitfields are **unconfirmed in the ryzen_smu driver source** (the amkillam v0.1.7 audit, P6-02); they stay zeroed → honest `Disabled`/`Na` (no field is ever displayed with an unverified mapping). Pending driver-side confirmation (§5.5).
- **SPD part numbers (Cycle 7, C7-02/03 — decode-internal, no wire shape change, §9.5):** the module part-number offset is **generation-dependent** — DDR3/DDR2 `0x81..0x91` (unchanged), **DDR4 `0x149..0x15D`** (the old 0x81 read was the DDR3 location — blank on DDR4), **DDR5 `0x200..0x220`** — and `XMP3_BASE` moved `0x200` → **`0x300`** (JESD79-5; the DDR5 part-number/profile collision fixed — part + XMP3/EXPO coexist).
- **The Cycle 6 GUI wire extensions (the sanctioned frozen-shape changes, D-C10/D-C11 — §9.9):** the `SystemPlatform` struct (DMI + `/proc`-sourced board/BIOS/AGESA + CPU clock, N/A-safe), `SystemMemoryTelemetry` `platform`/`total_capacity`/`dimm_sizes`, `SpdModule` `die_maker`/`die_type`/`devices`, and the command-rate path (`AmdPmSnapshot` raw slot → `SmnFields` (0x50200 bit 10) → `ClockReadout.command_rate`). All landed with the serde/bincode round-trip gates; no other frozen arm shapes touched.
- **The Cycle 7 burn-in wire extensions (D-1/D-2, the C7-07 atomic co-land — §9.9):** `Request += StartBurnIn { target: StreamTarget, duration_minutes: u32 }` (0 = infinite; stop via the existing `CancelBenchmark { run_id }`) + `Response += BurnInProgress(BurnInTick)` (one per completed bandwidth cell / per-tier latency pass of each iteration; `BurnInTick` = `iteration` + `elapsed_secs` + exactly one of `(op, gbps)` / `latency_ns`) — both arms appended, **every existing wire arm byte-identical** (the normal-bench stream unchanged), all four consumers co-landed with the guards.
- **Live result (5950X, Cycle 5 close):** MCLK/UCLK/FCLK = 1800 MHz OneToOne, VDDCR_SOC = 1.1375 V, **27/27 DRAM timings + GDM live and tick-identical to `monitor_cpu -m`** (31 active gates; VERDICT PASS).

## 5. Open Items for Cycle 9

1. **Operator live-run sign-off (the remaining manual gate of the PASS-WITH-MANUAL-LIVE-VERIFY verdict) — the 7 Cycle 8 checks:** `plans/CYCLE8-LIVE-CHECKLIST.md` (authored at close — the operator sign-off record; needs interactive sudo + the operator-attended display on the 5950X host): (a) **header line 2** reads `… (BIOS: 5601, SMU 56.78.0)` — **no `AGESA 56.78.0`** (D-1); (b) **RAM line** reads `RAM: 62.68 GiB (2x16 GiB) 3200 MT/s | Dual-Channel` — **not `4 GiB (2x2 GiB)`** (D-2/D-3); (c) **SPD part** reads `F4-3600C18-32GVK` from `0x149` (the byte-`0x02` classification fix — the Cycle 7 deferred check (iv) resolved); (d) **Zone 1 (AMD)** — no `gear` row; separate `GEAR_DOWN` (`Enabled`) + `CR` (`1T`) rows; every absent cell bare `N/A` in muted gray, the AMBER warnings still showing (D-4/D-5/D-6); (e) **Zone 2** — unmeasured / not-started cells gray bare `N/A` (not red); (f) **Zone 3** — card absent fields gray bare `N/A`, the daemon-disconnected line + the `! <error>` line **stay CRIMSON** (the fault-red preservation); (g) **Graphs window** — the `VDDCR_CPU` row reads `VDDCR_CPU — N/A` (bare note, AMBER series accent kept) — spawning/closing it on Wayland also re-runs the Cycle 7 deferred check (i), and the 1400×900 no-vertical-scroll layout (check (v)) rides the same run after the row changes. Plus the Cycle 7 deferred **burn-in live per-iteration updates (check (ii))** in the same run (a Run Burn-In against the real daemon streams per-iteration values, Cancel stops cleanly). A flapping/absent daemon degrades to the gray placeholders + the red status line — no crash. On pass: the QA verdict becomes a full PASS and the push gate opens (§5.2).
2. **Push to GitHub (operator go-ahead — after the live sign-off, §5.1).** Fast-forward `origin/v2-development` to **`5730b33`** (origin is at `b908f7b`, 178 behind — **NEVER force-push**); prune the **73** fully-merged remote chunk branches — the **17** `branch/chunk-p5-*` + **23** `branch/chunk-c6-*` + **22** `branch/chunk-c7-*` + **11** `branch/chunk-c8-*` (all 73 verified merged into the tip; the runbook is §8); optional **`v2.0.0`** tag (decide at push time, explicit sign-off — it fixes the AUR `git describe` pkgver; the PKGBUILD no-tag fallback keeps the package buildable either way). Until go-ahead: 100% local.
3. **O2 — Intel live MCHBAR decode (hardware-gated).** The i5-6600 is **not yet attached**. When it is: run the live decode (root daemon + `ramsleuth-client -- dump`, or `sudo cargo run -p ramsleuth-telemetry --release`); confirm per-channel tCL/tRCD/tRP/tRAS + command-rate/gear against known-good (the live SPD's JEDEC base rate + the BIOS config); the IMC register offsets are still **SKELETON** — the first live pass validates the decode pipeline and reconciles the offsets (plan D6: verify-first, reconcile-if-divergent; the wire `IntelChannel`/`IntelReadout` shapes never change); confirm Intel voltages/CAD = `Na(NotApplicable)`; re-verify the AMD-host zero-`/dev/mem` regression. P6-06 is parked for exactly this.
4. **O5 — MSRV decision (operator call).** Keep **1.75** vs bump **1.89**. Facts: workspace `rust-version = "1.75"`; all 441 lockfile packages MSRV ≤ 1.75; the AVX-512F bodies carry `#[clippy::msrv = "1.89"]` annotations but compile green on the CI 1.75 leg and are runtime-gated (never exercised on this AVX2-only host); the CI 1.75 leg is the continuous MSRV proof. If bump: the `rust-version` edit + CI matrix `["1.89","stable"]` + pin re-verification — a standalone follow-up micro-chunk after the decision (the plan's P6-07 docs-only record was parked, not authored).
5. **CAD/RTT/drive + PDM SMN bitfields (honest N/A — driver-side confirmation pending).** Unconfirmed in the ryzen_smu driver source (amkillam v0.1.7, audited in P6-02); the sanctioned confirm-or-Na position is held — no field displayed with an unverified mapping. Needs an AMD-published UMC register map or an upstream bitfield publication before the CAD/RTT/drive cells can be populated. Non-fatal: the O1 PASS treats the CAD gate as informational/deferred.
6. **AIDA64 parity gate (deferred to the DDR5-6000 AM5 host).** ±5% DRAM bandwidth + the 60–75 ns DDR5-6000 AM5 latency band (±5% of AIDA64 on matching hardware) — the 5950X host is DDR4 (measured DRAM latency ~81.5 ns is out of that band as expected; reported, not failed — §7c). No action until the AM5 host is available.
7. **Non-blocking follow-ups (Cycle 8 + C7 carry-forward, still open):** (a) the **TUI/CLI N/A twins restyle** — the D-4 scope ruling left `ramsleuth-tui` (`na_text`/`na_item`/`section_na`, verbose + `CRIMSON`) and `ramsleuth-client` (`na_cell`/`section_state`, verbose) in their old forms — a consistent bare-`N/A` + gray restyle across the terminals (scope it carefully — the CLI's column-aligned dump benefits from the reason text); (b) the **optional root-mode AGESA source** — a real AGESA string (SMBIOS Type 11 / the raw DMI table) requires root; a `dmidecode`/raw-table read behind a privilege gate would slot ahead of the BIOS scan in `read_agesa` (D-1's relabel stands until then); (c) the **G.Skill non-compliant SPD hub spec-annex audit** — the DDR4 module-specific section (`0x80..0x83` exact field map) to replace D-2's consistency/derivation workaround (the live shape is pinned as the regression — a tighter map only replaces the derivation); (d) the **`SpdModule.die_type` label mapping** — the field is wired (C6-02) but the label defaults to `Na` (the die maker is live); (e) **settings XDG persistence** — `GuiSettings` is in-memory only per process launch (the CLI `--socket` seeds it); (f) the **"q"-focus Quit guard** — the raw key scan fires `Q` even while the settings socket `TextEdit` has focus (skip the key dispatch while a text-edit widget holds focus). (The C6 Units/Theme formatters are **closed in Cycle 7** (C7-11/C7-15); the theme stays the single DarkSlate variant by design.)

## 6. ryzen_smu Installation (current state + how it is managed)

- **Upstream: `amkillam/ryzen_smu`** (branch `main`, **v0.1.7** — actively maintained, kernel 7.2+ fix, Zen 3 support; builds the `ryzen_smu` module, registers the `ryzen_smu_drv` kobject, ships a `dkms.conf` + the `monitor_cpu` userspace CLI). Upstream selection was reconciled in P5-08…P5-10: the original `53XU/ryzen_smu` is **DEAD (HTTP 404)** — the script warns about it explicitly — and `leogx9r/ryzen_smu` (original, frozen 2021) was rejected in favor of the active amkillam fork. The URL remains overridable via `RYZEN_SMU_URL`; **verify the upstream at setup time, do not trust a cached URL**.
- **Installed on this host (verified through Cycle 7 — unchanged: Cycles 6–7 are GUI/decode-only; the module, DKMS, and `monitor_cpu` are untouched):** `dkms status` → `ryzen_smu/1.d298366, 7.2.3-1-cachyos-custom, x86_64: installed`; `modinfo ryzen_smu` → v0.1.7 (author Leonardo Gates); module loaded (`lsmod`). Sysfs at `/sys/kernel/ryzen_smu_drv/`: `pm_table` (2288-byte blob), `pm_table_version` (`05 08 38 00` = 0x380805), `pm_table_size` (0x8F0 = 2288), **`smn` (rw — the Cycle 5 SMN channel, live-verified)**, `codename`, `drv_version`, `version` (= `56.78.0` — the host's AGESA source, Cycle 7 key finding 2), `hsmp_smu_cmd`, `mp1_smu_cmd`, `mp1_if_version`, `rsmu_cmd`, `smu_args`.
- **Ground-truth CLI: `/usr/bin/monitor_cpu`** (mode 700, root-only — the install script builds it from upstream's `userspace/` dir). The verified reference for the PM-table parse + the `-m` SMN one-shot. **Note (Cycle 5 finding #2):** its PM-frame path hard-gates on PM-table version 0x240903 and exits with ZERO frames unless `-f` is passed — this host's table is 0x380805, so the ground-truth script uses `monitor_cpu -f` (infinite frame loop; the `timeout` rc=124 kill is the normal capture path, finding #2 + P6-11).
- **Install / management: `scripts/install-ryzen-smu-dkms.sh`** — idempotent, operator-run (re-execs under sudo when not root):
  1. **Fast path:** `pm_table` present (canonical `ryzen_smu_drv` first, legacy `ryzen_smu` second) → "nothing to do", exit 0. (Re-runs after a kernel update hit this once DKMS `AUTOINSTALL=yes` has rebuilt + the module is loaded.)
  2. **Prereqs:** `pacman -S --needed dkms base-devel`; kernel build-tree guard — `[[ -d /lib/modules/$(uname -r)/build ]]` or stop with the candidate headers packages listed (**never guesses a custom-kernel headers package** — on this host: the `7.2.3-1-cachyos-custom` headers).
  3. **Clone:** shallow clone of the verified upstream → `/opt/ryzen-smu-src` (re-runs `git pull --ff-only`, tolerating offline).
  4. **Stage (P5-11):** copy `{LICENSE, Makefile, dkms.conf, drv.c, smu.c, smu.h}` into `/usr/src/ryzen_smu-$PKGVER` (`PKGVER` = `git rev-list --count HEAD`.`short-hash`); prefer the repo's `packaging/ryzen-smu-dkms/dkms.conf` (dual-location fallback: repo path, then installed `/usr/share/ryzen-smu-dkms/`), with its `PACKAGE_VERSION` sed-aligned to `$PKGVER`; write `/usr/lib/depmod.d/ryzen_smu.conf` (`override ryzen_smu /extra/ryzen_smu.ko`); build + install `monitor_cpu` → `/usr/bin` (non-fatal if absent).
  5. **DKMS (P5-12/13/14):** `dkms add ${MODULE}/${PKGVER}` (already-registered → continue) → `dkms build ${MODULE}/${PKGVER} -k ${KERNEL}` → install-state check via `dkms status` (state exactly `installed` → skip; `built` → proceed) → `dkms install ${MODULE}/${PKGVER} -k ${KERNEL}`. The staged `dkms.conf` uses the authoritative upstream pattern (`MAKE="make TARGET=${kernelver}"`; DKMS runs in `.../build`; `DEST_MODULE_LOCATION=/extra` matches the depmod override).
  6. **Load + verify:** `modprobe ryzen_smu` + `/etc/modules-load.d/ryzen_smu.conf` (boot persistence); verify `pm_table` present (canonical first, legacy second) with a `dmesg`/`dkms status` hint on failure. Without the module the app still works: `N/A (DriverMissing)`, exit 0, no panic.
- **Key carried facts:** the frozen `systemd/ramsleuth.service` **never loads the module** (it assumes loaded; the `.install`/DKMS `AUTOINSTALL` path handles it) — a bare install degrades gracefully per the no-panic contract. The AUR extra `packaging/ryzen-smu-dkms/` (P5-05) is a safe no-in-chroot provisioning package: it ships the `dkms.conf` + the helper as `/usr/bin/ryzen-smu-dkms-install`; the operator runs the helper on the target. We deliberately do **not** depend on the external AUR `ryzen-smu-dkms-git` package (self-contained tooling; only the module source is fetched from amkillam git at install time).

## 7. Verification Environment

### (a) Primary AMD dev host — Ryzen 9 5950X (Zen 3, Vermeer)

- 16C/32T, 64 MiB L3, **DDR4-3600 — a G.Skill F4-3600C18-32GVK kit (2× 16 GiB rank-1 DIMMs, 16 Gb dies; the SPD's 3200 MT/s *base* speed was the source of the earlier "DDR4-3200" misnomer — the kit's set-point is 3600 MT/s = 1800 MHz MCLK)**, AVX2 — **no AVX-512** (the `--avx512` path falls back to AVX2; the 512-bit kernels compile but the runtime gate selects AVX2).
- **OS: CachyOS, kernel `7.2.3-1-cachyos-custom`.**
- **Display: Wayland** (`DISPLAY=:0`) — the GUI's second-window (Graphs) spawn/close on this compositor is a deferred live check (§5.1).
- **`ryzen_smu` kernel module: INSTALLED + LOADED** (amkillam/ryzen_smu v0.1.7 via DKMS — §6). Live AMD telemetry is fully working: PM table (clocks 1800 MHz OneToOne, VDDCR_SOC 1.1375 V) + **SMN (27 DRAM timings + GDM live — tick-identical to the reference; CAD/RTT/drive honest N/A pending driver-side confirmation)** + SPD (G.Skill, 16384 Mbit, rank-1; part number from the DDR4 location `0x149`, Cycle 7).
- Ground truth: `sudo monitor_cpu` (`-f` for the PM frame, `-m` for the SMN one-shot; §6); the cross-check: `sudo scripts/amd-ground-truth.sh` (**VERDICT PASS**, Cycle 5 — §2).
- **The ramsleuth daemon on this host is run manually** (`sudo target/release/ramsleuth-daemon` — or `--socket /tmp/ramsleuth.sock` for an unprivileged dev socket) — **no `systemd` unit is installed**; the unit + AUR packaging (§6, §11) are the distribution path, not the dev host.

### (b) Intel test machine — i5-6600 (Skylake)

- LGA-1151, Intel Core i5-6600 (6th-gen Skylake), **dual-channel (2 DIMM channels)** — matches `channel_count(Skylake) = 2` in the Intel decode model. **Not yet attached to the dev environment** (the O2 hardware gate — open item 3).
- Purpose: **live Intel MCHBAR decode verification** (BAR5 → read-only `/dev/mem` mmap → per-channel IMC registers). The IMC offsets are still SKELETON — the first live pass also reconciles them (plan D6). The Phase 2 Intel path is Intel-gated and returns `N/A (UnsupportedHardware)` on the AMD host.
- Intel voltages/CAD-bus sections are out of Phase 2 scope by design → `Na(NotApplicable)` on Intel.

### (c) Deferred target

- The AIDA64 parity gate (±5% DRAM bandwidth; 60–75 ns DDR5-6000 AM5 latency band) is deferred to a **DDR5-6000 AM5 host** — the 5950X host is DDR4 (measured DRAM latency ~81.5 ns is out of that band as expected; reported, not failed). Open item 6.

## 8. Push Policy & Git State

- **Git state (2026-09-16, verified):** local `v2-development` @ **`a2577c0`** is **140 commits ahead of `origin/v2-development` @ `b908f7b`** (the P5-15 review/merge commit — everything up to `b908f7b` is on origin: all 15 P5 chunks + the 2 earlier fixes, fast-forwarded during Cycle 4, no force-push). The 140 = the 95 Cycle-5/Cycle-6 commits (as recorded at the Cycle 6 close: the 19 Cycle-5 + the 76 Cycle-6) + the **45 Cycle-7 commits** (`ca46982` the C6 compaction + the 22 C7 feat/merge pairs; Rust delta `33dd08b..a2577c0`: 17 `.rs` files — the full 21-file diff incl. the compaction docs: +6666/−900). All are **local-only, unpushed** (operator-gated). Working tree: **no code** — `DEV_LOG.md` modified (the Cycle 7 per-lease record, +87/−5), `MASTER_LOG.md` modified (the Cycle 7 section appended at the top, +68 lines), `plans/PLAN-CYCLE7.md` untracked (the Cycle 7 plan — the `CYCLE7-LIVE-CHECKLIST.md` referenced by the plan §7.6 was **not authored**; the deferred live checks are §5.1), + this handover refresh — all uncommitted (the Cycle 7 compaction lands them). **No local chunk branches remain** (the 22 c7 branches pruned after merge, as the c6/p6 ones); **single worktree** (the c7 worktrees cleaned up).
- **Remote chunk branches on origin — all 62 verified fully merged into `v2-development`** → prune candidates on go-ahead: the **17** `branch/chunk-p5-*` (`p5-01`, `p5-02`, `p5-02-fix`, `p5-03`, `p5-04`, `p5-04-fix`, `p5-05`, `p5-06`, `p5-07`, `p5-08-sysfs`, `p5-09-script`, `p5-10-readme`, `p5-11-dkms`, `p5-12-dkmsconf`, `p5-13-dkmsver`, `p5-14-dkmsinstall`, `p5-15-pmtable`) + the **23** `branch/chunk-c6-*` (`c6-01`…`c6-30` — one remote ref per merged branch, the fixture-ripple refs each covering their sibling chunk IDs) + the **22** `branch/chunk-c7-*` (`c7-01`…`c7-22` — one remote ref per merged branch). **Note (carried from the Cycle 6 close):** the committed `MASTER_LOG.md` Cycle 6 section still contains the **reversed c6 remote-branch sentence** ("No remote `branch/chunk-c6-*` branches exist" — the verified state is 23 remote c6 refs, zero local); correct that sentence when the Cycle 7 section is compacted.
- **`origin/master` = divergent legacy (untouched).** The operator confirmed `v2-development` is the canonical line — **no need to preserve or branch from `master`**; treat upstream (`https://github.com/MadGoatHaz/RamSleuth`) as a read-only reference. Never merge into or force onto it.
- **Push policy: LOCAL only until the operator's explicit go-ahead — do not push, do not delete any remote branch (no exceptions).** On go-ahead, in order: (1) **fast-forward** `origin/v2-development` to **`a2577c0`** (NEVER force-push — the push is a strict ff; if `origin` is not a strict ancestor, stop and report); (2) prune the 62 fully-merged remote chunk branches (`git push origin --delete <branch>` — every one verified merged; the `--no-ff` merge history lives on `v2-development`); (3) **optional tag** (e.g. `v2.0.0`) — decide at push time, with explicit sign-off.
- **Logs:** `DEV_LOG.md` is the active lease board (**no active leases**; the Cycle 7 per-lease record is in the working tree, uncommitted; CURRENT_STATE = Cycle 7 COMPLETE, ready for the v2.0.0 push); `MASTER_LOG.md` holds the durable per-cycle history (Cycles 1–6 at HEAD; the Cycle 7 section is present in the working tree (uncommitted); the "newest cycle first" reordering is a compaction candidate).

## 9. Key Architectural Decisions & Frozen Interfaces (carried forward — still accurate)

1. **No-panic `Section<T>` contract (P2-02, frozen):** displayable telemetry values are `Section<T> { Value(T), Na(TelemetryError) }`; fallible operations return `TelemetryResult<T>`. `TelemetryError` variants: `UnsupportedHardware`, `UnsupportedVendor`, `DriverMissing`, `InsufficientPrivilege`, `UnknownPmTableVersion`, `NoDevmem`, `NotApplicable`, `InvalidValue(String)`, `Io(String)`. **No `panic!`, no `unwrap()`/`expect()` on hardware-derived data, no unguarded deref** anywhere in the telemetry crate; every `unsafe` block carries `// SAFETY:` and is bounds-checked. The CLI renders every missing value as `N/A (<reason>)` and exits 0.
2. **Direct SMU access — no `ryzen_smu` Rust crate (P2-03, decision D1):** sysfs-first read of the PM table — canonical `/sys/kernel/ryzen_smu_drv/pm_table` (P5-08) with the legacy `/sys/kernel/ryzen_smu/pm_table` as a secondary candidate — plus the sibling `pm_table_version` / `pm_table_size` attrs (§4); fallback open of `/dev/ryzen_smu` + driver ioctl via `nix` (53XU-fork tolerance only). We own the version-guarded PM parse and all mapping. `nix` (features `fs`/`ioctl`/`mman`) is the **only** Phase 2 dependency and the only telemetry dep. Rejected: `ryzen_smu` crate, `memoffset`, serde/serde_json (hand-rolled JSON in the telemetry CLI), clap (std `env::args`).
3. **AMD model FULLY RECONCILED (P5-15 PM table, Cycle 4 + P6-02/03/10 SMN path, Cycle 5 + the command-rate slot, Cycle 6 + the AGESA validator, Cycle 7, §4):** the f32 layout + `TableVersionId` sets are live-verified (1800 MHz + 1.1375 V on the 5950X) and the **27 DRAM subtimings + GDM are now live from the SMN registers** (tick-identical to the `monitor_cpu` reference — VERDICT PASS); the **command rate (0x50200 bit 10) is stored + displayed since Cycle 6** (`ClockReadout.command_rate`, 1T/2T, Intel honest `Na`). **Intel IMC register offsets (P2-07) remain plan-mandated SKELETONS** pending live validation on the i5-6600 (open item 3). CAD/RTT/drive + PDM are honest `Na` until their bitfields are confirmed driver-side (open item 5).
4. **Intel MCHBAR gating (P2-06, decision D4):** `CpuInfo` must be Intel before **any** file/mmap access; `/dev/mem` is mapped **read-only** behind a guard struct that bounds-checks reads and `munmap`s in `Drop`; STRICT_DEVMEM EIO/ENODATA → `InsufficientPrivilege`; missing file → `NoDevmem`. On the AMD host the path returns `Na(UnsupportedVendor)`/`Na(UnsupportedHardware)` and verifiably never touches `/dev/mem`.
5. **SPD is unprivileged (P2-08 / P2-09):** raw `ee1004` sysfs images (512 B DDR4 / 1024 B DDR5); JEP106 module + die makers (continued-ID support), rank bits, part/serial, JEDEC speed, XMP 2.0/3.0 + EXPO profile summaries; a checksum failure skips the profile, never the module; no panic on truncated/garbage data. Live codes reconciled in Cycle 5: density `0x0D` → 16 Gb (vendor/legacy), maker `0xC1` → G.Skill. Cycle 6 (C6-02): `SpdModule` carries `die_maker` / `die_type` / `devices` (the die-type label mapping defaults to `Na` until confirmed — the GUI follow-up list). **Cycle 7 (C7-02/03): the module part-number decode is generation-dependent** — DDR3/DDR2 `0x81..0x91` (unchanged), DDR4 `0x149..0x15D`, DDR5 `0x200..0x220` — and `XMP3_BASE` moved `0x200` → `0x300` (JESD79-5, the DDR5 part/profile collision fixed); decode-internal, the `SpdModule.part`/`serial`/`profiles` shapes are untouched.
6. **Phase 1 checksum conventions (frozen):** read/copy kernels return the **wrapping sum of the buffer's little-endian 64-bit words** (data-sensitive DCE sink; `0` for a zeroed buffer); the scalar fallback has identical semantics; write kernels' frozen return is the **byte counter** (so `checksum == total_bytes` for Write); per-slice word-sums wrapping-add to exactly the whole-buffer checksum (proves the partition has neither overlap nor gap); all tier buffers are 64-byte aligned (`AlignedBuf`); bandwidth cells are **best-of-3** `Instant` timings, latency cells the **median** of 3 runs of a ≥1 M-hop chase over the *materialized* full-tier ring (Memory tier exceeds L3 → true DRAM latency); non-x86_64 latency falls back to `Instant` only (documented precision loss). Cycle 5 (P6-05): sub-4 MiB tiers re-run their kernel `iters = clamp(32 MiB/total, 1..=65536)` times per pass (work/pass ≤ 32 MiB; aggregate = `iters ×` the single-pass value — the per-pass convention is unchanged; large tiers `iters = 1` byte-identical).
7. **CPUID family mapping (P2-01, frozen):** the 5950X reference host reports **family `0x19` → `Amd(Zen3)`** (frozen map: `0x15`→Zen 1, `0x17`→Zen 2, `0x19`→Zen 3, `0x1A`→Zen 4, `0x1C`→Zen 5, else `Unknown`). **Note: desktop Zen 4/5 silicon also reports family `0x19` on some boards, so the AMD PM parse (P2-04) keys on the SMU version — now the exact `TableVersionId` sets (§4) — not on `AmdZen`** — generation ambiguity cannot break the PM layout. Intel: family `0x6` model table covers Skylake (`0x4F`/`0x56`/`0x5E`) through Arrow Lake; unrecognized models → `IntelGen::Unrecognized` (still Intel-gated, not `Unknown`).
8. **Topology & buffers are pure (P1-02 / P1-03):** `detect() -> Result<CpuTopology, TopologyError>` over /sys (SMT siblings filtered; per-CCD L3 slices; total L3); `plan(&CpuTopology) -> BufferPlan` is a pure function (L1 16 KiB, L2 256 KiB, L3 = 50% of a CCD slice, DRAM = max(256 MiB, 3× total L3), latency ring 128 MiB; all 64-byte aligned; safe fallbacks when sysfs fields are missing).
9. **Phase 3/4 privilege + wire contract (Cycle 3, frozen):** one privileged daemon is the **sole privilege boundary** — all hardware I/O (ryzen_smu sysfs/char-dev, MCHBAR `/dev/mem`, ee1004) stays daemon-side; the client/TUI/GUI chain is tokio-free and never touches hardware. Wire: synchronous length-prefixed **Bincode frames** with a **16 MiB `MAX_FRAME_SIZE` guard** over a **0660 Unix socket** (`/run/ramsleuth/ramsleuth.sock`); tokio confined to the daemon (async accept loop + `spawn_blocking` pumps); serde derives on every wire-crossing public type (payloads reused verbatim — no duplication, no boxing of frozen arm shapes). The daemon **inherits the no-panic contract**: any absent driver/privilege/hardware surfaces as structured `Na` sections in the payload — never a crash of the service or the clients. **Cycle 5 added no wire changes** — the SMN data entered the frozen `AmdPmSnapshot` in place (the P5-15 precedent: semantics changed, shape frozen). **Cycle 6 extended the wire for the GUI** (the sanctioned frozen-shape changes, D-C10/D-C11): the `SystemPlatform` struct + `mem_total_gib`, `SystemMemoryTelemetry` `platform`/`total_capacity`/`dimm_sizes`, `SpdModule` `die_maker`/`die_type`/`devices`, and the command-rate path (`AmdPmSnapshot` → `SmnFields` (0x50200 bit 10) → `ClockReadout.command_rate`) — all with the serde/bincode round-trip gates; no other frozen arm shapes touched. **Cycle 7 extended the wire for burn-in (D-1/D-2, the C7-07 atomic co-land):** `Request += StartBurnIn { target, duration_minutes }` (0 = infinite; stop via the existing `CancelBenchmark { run_id }`) + `Response += BurnInProgress(BurnInTick)` — both arms appended, **every existing arm byte-identical** (the normal-bench stream unchanged), all four consumers (client/TUI/GUI/daemon) co-landed with the guards.
10. **The SMN overlay (P6-02/03/10, frozen in Cycle 5; `command_rate` added in Cycle 6):** SMN data enters the frozen snapshot via `apply_smn(&mut snap)` — an **infallible no-panic overlay** that writes **only** `gdm` + the 27 timings + `command_rate` (PM fields, PDM, CAD never touched); DriverMissing = whole-overlay no-op; per-register containment zeroes only the failed register's fields; the driver's `0xffffffff` failed-read sentinel is **never decoded** (probe arm + `read_word` collapse + decode-level `word_at` normalization); the +0x100000 two-stage offset is decided by the **first read only**; the tRP mask is 6 bits at bit 16 (21:16) per the driver reference.
11. **The GUI threading model (Cycle 6, frozen; refined in Cycle 7):** one `RwLock` over the shared `TelemetryData` — the background poller is the **only writer** of the data fields (D6: no render-thread I/O), the render thread is the **only writer** of `settings` (its one permitted write, the settings panel), one guard at a time per thread (no lock-ordering deadlock); the bench command channel + cancel are separate; the poller re-reads the live `settings` (poll interval, refresh gate, socket) every tick — a settings edit retargets the **same running poller**, no restart; the keyboard path (fresh key-downs only, `repeat: false`) is routed through the same `handle_action` as the buttons (behavioral equivalence, D-C7). 1400×900 @ ~60 FPS, no UI-thread blocking. **Cycle 7 (the v2.0.0 polish):** `refresh_enabled` **defaults false** — the poller is **baseline-once** (exactly one fetch per distinct socket, thread-local `baseline_for`, D-8) then gated on the live knobs; the settings combos carry **unique `from_id_source` ids** (the `from_label("")` shared-Id desync is gone, C7-10); the header lines + the Zone 1 clock rows **consume the capacity/clock unit knobs** (C7-11/C7-15); and the **Graphs window** (item 12) is a second native viewport that is a **pure reader** of the shared `TelemetryData` (D6: the poller remains the only writer).
12. **The Graphs window (Cycle 7, frozen):** eframe 0.27.2 **native multi-viewport** — the root re-issues `show_viewport_deferred(GRAPHS_VIEWPORT_ID, …)` **every frame while open** (idempotent — a stable builder produces no delta); the child is a real winit OS window driven by the **one existing event loop** (the glow runtime's desktop path, `set_embed_viewports(false)`), sharing the root `egui::Context` + the `Arc<RwLock<…>>` state as a pure reader, with its own 16 ms repaint cadence; close = the root stops registering (the button clears the shared `Arc<AtomicBool>`) or the child callback sees `ViewportInfo::close_requested()` (the WM X) → the child goes un-`used` → egui's `end_frame` GCs it (the button + the WM X are behaviorally identical, D-C7). The D-3b dedicated-thread fallback (`eframe::run` on a second OS thread) is **rejected by design** — winit's event loop + the Wayland `wl_display` connection are thread-affine (the operator host is Wayland), and it would duplicate the eframe lifecycle/context/style; it remains the documented fallback only if the live gate fails (it has not — the D-3 live run is §5.1). The graph state = a 5-series ring (1800 samples = 60 min @ the 2 s poll, reusing the `history.rs` RingBuffer) recorded by the poller (one sample per successful poll, Na-guarded — no all-NaN holes); the bandwidth series is a **step series** from `latest_memory_read_bw` (D-5 — not a continuous sampler, which would monopolize every pinned core); VDDCR_CPU + CPU temp have no source on this host → honest `N/A (no source)` rows (D-4); the interactivity (window minutes 1/5/15/60, drag-pan, hover crosshair) is session-local render state in the child context's `IdTypeMap`.

## 10. Workspace Layout (7 crates)

| Crate | Status | Contents |
|---|---|---|
| `crates/ramsleuth-bench` | **DONE (Phase 1) + L1/L2 refinement (P6-05) + Cycle 7 burn-in engine** | `features` (AVX2/AVX-512F runtime detect), `topology` (/sys physical-core enumeration, SMT filter, total/per-CCD L3), `buffers` (pure 64B-aligned sizing plan), `kernel_read` / `kernel_write` / `kernel_copy` (AVX2 streaming / NT / copy), `kernel_512` (AVX-512F variants + AVX2 fallback), `worker` (pinned barrier-synced per-core dispatch + sub-4 MiB inner-loop iterations), `latency` (pointer-chase ring, `__rdtscp`), `orchestrator` (4×4 grid, best-of-3 / median), `streamed` (the wire-ready streaming bench — `run_streamed` + the Cycle 7 `run_burn_in` duration engine with per-iteration `BurnInTick` emits; the behavior-identical `run_cell_pass` extracted for sharing), `main` (verification CLI; hand-rolled JSON, no serde/clap). Deps: `libc` only. |
| `crates/ramsleuth-telemetry` | **DONE (Phase 2) + model fully reconciled (Cycles 4–5) + Cycle 6 wire extensions + Cycle 7 decode refinements** | `cpuid` (vendor + frozen generation map), `error` (`TelemetryError` / `Section<T>` no-panic contract), `amd_smu` (sysfs candidate list — `ryzen_smu_drv` first; sibling `pm_table_version`/`pm_table_size`; `/dev/ryzen_smu` ioctl fallback), `amd_pm` (live-verified f32 PM layout + `TableVersionId` sets + the C6-04 `command_rate` raw slot, §4), **`amd_smn` (the SMN sysfs accessor + the verified 13-register bitfield table + the no-panic overlay — 27 timings + GDM live on the 5950X; sentinel-safe; + `command_rate` (0x50200 bit 10) since C6-05)**, `amd_readout` (shared display types + AMD mapping — `ClockReadout.command_rate` since C6-03), `intel_mchbar` (PCI BAR5 + read-only `/dev/mem` mmap guard), `intel_readout` (per-channel IMC decode — offsets still skeleton), `spd_eeprom` (ee1004 unprivileged acquire), `spd_decode` (JEP106 / rank / XMP / EXPO — `0x0D`/`0xC1` reconciled P6-04; die maker/type/devices since C6-02; **Cycle 7: per-generation module part decode (DDR4 `0x149..0x15D` / DDR5 `0x200..0x220`) + `XMP3_BASE` `0x300` (JESD79-5) + the AGESA validator group2 bound relaxed to 1–6 digits (D-6) — all decode-internal, no wire shape change**), `facade` (`SystemMemoryTelemetry` + `collect()` — `acquire → parse → apply_smn → map`; + `platform`/`total_capacity`/`dimm_sizes` since C6-06), `platform` (DMI + `/proc`-sourced board/BIOS/AGESA + CPU clock, N/A-safe; the smu `version`-attribute AGESA fallback since D-6), `main` (verification CLI). Deps: `nix` 0.29 (`fs`/`ioctl`/`mman`) only. |
| `crates/ramsleuth-protocol` | **DONE (Phase 3) + the C6 data-model fixture ripple + Cycle 7 burn-in wire** | Wire contract: `Request` / `Response` / `BenchMode` / `Message` serde enums (payload types reused verbatim from telemetry + bench), `DEFAULT_SOCKET_PATH` (`/run/ramsleuth/ramsleuth.sock`), length-prefixed Bincode frame codec with a 16 MiB `MAX_FRAME_SIZE` guard (`encode_frame` / `decode_frame` / `Frame` / `FrameError`). Zero `unsafe`, tokio-free. The C6 wire extensions (the `SystemPlatform` / capacity / die / command-rate shapes, §9.9) round-trip through the frozen frames. **Cycle 7 (D-1/D-2):** `Request += StartBurnIn { target, duration_minutes }` (0 = infinite) + `Response += BurnInProgress(BurnInTick)` — both arms appended, every existing arm byte-identical; the serde/bincode round-trip gates ride the codec. |
| `crates/ramsleuth-daemon` | **DONE (Phase 3) + the C6 fixture ripple + Cycle 7 burn-in job** | `caps` SOFT privilege probe (geteuid + `CapEff` bit 21; never panics/exits), `socket` listener (mode **0660**, stale-file probe → live `AlreadyRunning` / dead rebind, best-effort `chown`), TTL `TelemetryCache` (injectable collector), single-flight `BenchJobManager` (clean cancel + per-cell progress), `rpc` (per-connection async over the frozen wire contract), `main` bin (`--socket` / `--max-age`, SIGTERM/SIGINT → graceful stop + socket removal), `systemd/ramsleuth.service` (CAP_SYS_RAWIO-clamped, sandboxed unit). Deps: tokio (daemon-only). **Cycle 7:** `JobEvent += BurnInTick(BurnInTick)` + `BenchJobManager::start_burn_in(target, duration_minutes, threads)` — the same single-flight slot/claim/`run_id`/cancel-flag machinery as `start` (a burn-in and a normal bench are mutually exclusive); the job runs `run_burn_in` on `spawn_blocking` and pumps `JobEvent::BurnInTick` + exactly one terminal (`Result` = the last pass's grid / `Cancelled` / `Error`). |
| `crates/ramsleuth-client` | **DONE (Phase 3) + the C6 fixture ripple + Cycle 7 guards** | Synchronous `UnixStream` transport (read/write timeouts, 3-retry backoff, friendly `DaemonDown` diagnostics); pure `dump` dashboard renderer (full hardware timings, every N/A cell with its reason — **the Phase 3 exit criterion**); `bench` / `status` commands; CLI (`dump` / `bench` / `status` + `--socket` / `--tier` / `--mode`, exit codes 0/1/2). std-only, no tokio — the transport shared by the CLI and by `tui`/`gui`. **Cycle 7:** the `bench()` loop guards the new `Response::BurnInProgress` arm as a protocol violation (a contract check for the normal-bench subcommand); the CLI gains no burn-in subcommand this cycle (documented follow-up — the GUI is burn-in's consumer). |
| `crates/ramsleuth-tui` | **DONE (Phase 4) + the C6 fixture ripple + Cycle 7 guards** | ratatui 0.29 + crossterm 0.28 (MSRV ≤ 1.75 via two lockfile pins); pure `key_to_action` (R / S / Q); 3-zone non-scrolling layout (timing matrix / bench grid + progress / SPD + daemon status); terminal loop with a background 2 s updater. **Cycle 7:** the GetTelemetry match's 4-arm bench guard extends to the `BurnInProgress` arm (no behavior change for the TUI). |
| `crates/ramsleuth-gui` | **COMPLETE (Cycles 6–7 — the full Grand Design §3.1/§3.2 dashboard + the v2.0.0 polish)** | egui / eframe / egui_extras 0.27.2 (the pinned 1.75-compatible line — **no new dependencies**; the Graphs window rides the native multi-viewport API, §9.12); semantic palette (cyan / amber / slate / crimson); the **3-line header** (platform tag; CPU + motherboard/BIOS/AGESA — `56.78.0` on this host via the smu `version` fallback, D-6; RAM total + per-DIMM + channel mode + sync-mode — the lines + the Zone 1 clock rows consume the capacity/clock **unit knobs**, C7-11/C7-15); **Zone 1** 3-column compact (all six sections at 1400×900, **no vertical scroll**, C7-12) + the **GDM/CR row** (`GEAR_DOWN: Enabled/Disabled · CR: 1T/2T`, C7-13, D-7) + **vendor-conditional blocks** (the unsupported-vendor N/A block omitted, C7-14); **Zone 2** 4×4 bench grid with per-cell live fill + **flat status line** (`Status: Idle` / `Running… <m:ss>` / `Done`, no pill — C7-17) + **burn-in mode** (minutes input with 0 = infinite, Run Burn-In, **live per-iteration row + the grid wired to `burn_in.latest`**, Cancel stops it — C7-16/C7-18); **Zone 3** SPD cards (product line, **per-generation part number** — DDR4 `0x149` / DDR5 `0x200`, C7-02/03, DRAM die maker/type, human rank label) + daemon status naming the live socket; **polling = baseline-once** (auto-refresh **OFF by default** — one fetch per distinct socket regardless of the refresh gate, D-8; enabling refresh resumes the cadence) + the settings panel (`GuiSettings`: poll interval, refresh gate, units/theme, socket — in-memory; the combos carry **unique `from_id_source` ids** — the desync fix, C7-10; a poll/socket edit retargets the running poller, no restart); **the Graphs window** — a dedicated second OS window (native multi-viewport, §9.12): 5 series (CPU freq / VDDCR_CPU / VDDCR_SOC / CPU temp / mem bandwidth), hover crosshair + timestamp tooltip, horizontal pan, 1/5/15/60-min window (C7-20/C7-21/C7-22); the **embedded trend strip is removed** (C7-19 — `TelemetryData.history` + `record_history_sample` kept for the graph state's reuse); **F2** = 640×420 validation-card PNG (title/timestamp + CPU/RAM lines), **F3** = JSON (the frozen wire root + a top-level `bench` key) to `$HOME`; **keyboard F2/F3/Q** (fresh key-downs only, the same `handle_action` path as the buttons); 1400×900 @ ~60 FPS eframe app, no UI-thread blocking. **Non-blocking follow-ups (Cycle 8 micro-chunks if desired, §5.7):** (a) the "q"-focus Quit guard (typing `q` while the socket `TextEdit` has focus quits); (b) the `SpdModule.die_type` label mapping (defaults `Na` — the die maker is live); (c) settings XDG persistence (in-memory today). |

Root `Cargo.toml`: **7 members**, `version 0.1.0`, `edition 2021`, `rust-version = "1.75"`, license MIT, repo `https://github.com/MadGoatHaz/RamSleuth`. Packaging: `packaging/` (AUR `ramsleuth-git` + optional `ryzen-smu-dkms` extra + README), `scripts/install-ryzen-smu-dkms.sh` + `scripts/amd-ground-truth.sh`, `.github/workflows/ci.yml`, `systemd/ramsleuth.service` (frozen).

## 11. How to Run (quick reference, release)

The QA-verified flow: one privileged daemon; all frontends unprivileged over the socket.

```bash
# Build everything
cargo build --workspace --release

# 1) Daemon — default socket /run/ramsleuth/ramsleuth.sock (root, CAP_SYS_RAWIO via the unit)
sudo target/release/ramsleuth-daemon
#    or unprivileged local dev (0660 socket, SOFT caps probe warns and keeps serving):
target/release/ramsleuth-daemon --socket /tmp/ramsleuth.sock
#    (the dev host runs the daemon this way, manually — no systemd unit installed; §7a)

# 2) CLI client — no sudo needed
target/release/ramsleuth-client -- dump                       # full hardware timings (exit-criterion command; AMD: clocks + VDDCR_SOC + 27 timings + GDM live on this host)
target/release/ramsleuth-client -- status                     # daemon + hardware status
target/release/ramsleuth-client -- bench --tier l3 --mode memory-only
#    with a dev socket:
target/release/ramsleuth-client --socket /tmp/ramsleuth.sock -- dump
#    no daemon running → exit 1 + friendly DaemonDown start hint (never a crash)

# 3) TUI (R = refresh, S = snapshot, Q = quit) / 4) GUI (the full dashboard: refresh OFF by default — one baseline fetch on connect; the Graphs button spawns the second-window trend view; F2 = validation-card PNG, F3 = JSON + bench, Q = quit — keyboard + buttons; the Settings panel)
target/release/ramsleuth-tui
target/release/ramsleuth-gui

# Ground truth (the Cycle 5 cross-check — VERDICT PASS; needs the daemon up + root)
sudo scripts/amd-ground-truth.sh
#    raw reference: sudo monitor_cpu   (PM frame: -f — REQUIRED on this 0x380805 table; SMN one-shot: -m)

# Direct verification CLIs (no daemon; from the Cycle 1/2 lineage)
cargo run -p ramsleuth-bench --release            # [--avx512] [--json]
sudo cargo run -p ramsleuth-telemetry --release   # [--json]; root + ryzen_smu (installed) for live AMD

# systemd (unit shipped at systemd/ramsleuth.service; packaged by the AUR ramsleuth-git — distribution path; the dev host uses the manual run above)
sudo cp systemd/ramsleuth.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now ramsleuth

# Packaging (P5-02…P5-07): makepkg -si from packaging/ramsleuth-git/; optional AMD extra via scripts/install-ryzen-smu-dkms.sh (already installed on this host — §6)
```

`--tier` accepts `memory | l1 | l2 | l3 | full` (default `full`); `--mode` accepts `full | memory-only` (default `full`).

## 12. Operating Protocol Summary (how work runs in this repo)

1. **Strict phased gating:** each cycle compiles + runs + passes its exit criteria before the next starts. Each phase is planned first (`plans/PLAN-PHASE<N>.md`; the Cycle 6 GUI workstream was planned as `plans/PLAN-CYCLE6.md`, Cycle 7 as `plans/PLAN-CYCLE7.md`) with micro-chunks, dependency tags (`[ISOLATED]` / `[COUPLED-TO: …]` / `[CRITICAL-PATH]`), frozen-interface markers, and per-chunk quality gates.
2. **Single-file micro-chunking:** one target source file per chunk, ≤ ~50–100 lines changed; each chunk adds exactly one `mod <name>;` wiring line to `src/lib.rs` (wiring not counted against the budget).
3. **Per-chunk flow:** implement on `branch/chunk-<ID>` (forked from `v2-development`) → in-file unit tests (`#[cfg(test)]`) → clippy `-D warnings` → **per-chunk code review** (lease-signed) → `git merge --no-ff` into `v2-development` → branch pruned. Interface-freeze chunks (`[CRITICAL-PATH]`) merge first; no silent signature changes — changes are a plan edit + rebase. (Cycle 6 refinement, D-C10: a test-fixture-only red window is allowed during a sanctioned wire freeze, closed by the scheduled fixture-ripple chunks with a hard workspace gate at wave end. Cycle 7 scaled it: C7-07 co-landed every production match-site guard in one atomic commit so the workspace never went production-red.)
4. **Lease board:** `DEV_LOG.md` `@@@ ACTIVE_WORKERS @@@` — sign in with target files before work; sign out with `[DONE]`/`FAILED` + one-line decision + ahead-note; update `@@@ CURRENT_STATE @@@`. (Board is empty right now — the Cycle 7 per-lease record is in the working tree, uncommitted.)
5. **Handoffs:** 3-line **Semantic Pulse** — `STATUS` / `DECISION` / `AHEAD`, one short line each. No full-file dumps or code in chat; all detail goes to disk (plans, logs, docs).
6. **QA audit per cycle:** independent pass of every runnable gate (build, tests debug+release, clippy, live CLI runs in multiple privilege/CPU states) → then **compaction**: merge history into `MASTER_LOG.md`, prune chunk branches, reset `DEV_LOG.md`. (Cycle 7: QA audit = PASS-WITH-MANUAL-LIVE-VERIFY — the sudo/display live run is operator-attended; the compaction is in the working tree, uncommitted — §8.)
7. **Safety/fallbacks (hard rule):** unsupported CPU, missing driver, missing privilege, unknown PM-table version, absent `/dev/mem`, failed SMN read (the `0xffffffff` sentinel) → structured `Na(<reason>)`; **never panic, never segfault, never `unwrap`/`expect` on hardware data**; the Intel path never touches `/dev/mem` on non-Intel hardware; non-x86_64 compiles to a documented degraded path.
8. **MSRV:** keep the workspace at **1.75** and the lockfile pins intact unless the decision in §5.4 (O5) is made; the CI `1.75` leg is the continuous MSRV proof.
9. **Docs discipline:** `Docs/` holds the roadmap + spec + this handover + the full scope reference (`FULLSCOPEvsCOMPLETED.md`); `plans/` holds per-phase plans; `MASTER_LOG.md` is the durable record. Update them every cycle; this document is the standing handover — refresh it at each cycle close-out.

## 13. First Actions for the New Director (Cycle 9)

1. **Confirm the baseline** (re-verified at Cycle 8 close): `v2-development` @ `5730b33` with no code in the working tree; `cargo test --workspace` (debug + release) → **527/527**; `cargo clippy --workspace --all-targets -- -D warnings` → zero; `cargo build --workspace --release` → 6 binaries OK; `Cargo.toml`/`Cargo.lock` zero diff over `9ea4b3b..5730b33`. Then finish the **Cycle 8 compaction** (in flight in the working tree): review + commit it — the `DEV_LOG.md` Cycle 8 lease record (base line `5730b33`, ACTIVE_WORKERS cleared, per-lease history archived), the **`MASTER_LOG.md` Cycle 8 section** (the 11-chunk feat/merge record + the D-1…D-6 decisions + the QA + push state), the untracked `plans/CYCLE8-LIVE-CHECKLIST.md`, and this handover refresh — one local commit, **no push** (the push is §13.3, operator-gated after the live sign-off).
2. **Operator live-run sign-off** (the remaining manual gate, §5.1): `plans/CYCLE8-LIVE-CHECKLIST.md` on the 5950X host — the 7 Cycle 8 checks (the header `SMU 56.78.0` relabel, the `RAM: 62.68 GiB (2x16 GiB)` line, the `F4-3600C18-32GVK` part from `0x149`, the Zone 1 no-`gear`-row + `GEAR_DOWN`/`CR` split + the gray bare N/As, the Zone 2/3 gray N/As with the fault-red preserved, the Graphs `VDDCR_CPU — N/A`) plus the two Cycle 7 deferred checks that ride the same run (the burn-in live per-iteration updates; the 1400×900 no-vertical-scroll re-confirm after the row changes — the Graphs Wayland spawn/close is check (g)'s vehicle). On pass: the QA verdict becomes a full PASS.
3. **Push (on go-ahead after the live sign-off, §8):** fast-forward `origin/v2-development` to `5730b33` (178 ahead of `b908f7b`; NEVER force-push) → prune the 73 fully-merged remote chunk branches (17 `chunk-p5-*` + 23 `chunk-c6-*` + 22 `chunk-c7-*` + 11 `chunk-c8-*`) → optional `v2.0.0` tag with explicit sign-off.
4. **Operator/hardware gates (do not block):** O2 Intel (attach the i5-6600 → run P6-06: live MCHBAR decode, reconcile the skeleton IMC offsets); O5 MSRV (decide 1.75 vs 1.89; if bump, a follow-up micro-chunk).
5. **The non-blocking follow-ups, as small micro-chunks if desired (§5.7):** (a) the TUI/CLI N/A twins restyle (bare `N/A` + gray, scoped to keep the CLI dump's reason text); (b) the optional root-mode AGESA source (behind a privilege gate); (c) the G.Skill SPD hub spec-annex audit (the `0x80..0x83` field map); (d) the `SpdModule.die_type` label mapping (defaults `Na`); (e) settings XDG persistence (in-memory today); (f) the "q"-focus Quit guard (skip the key dispatch while a text-edit widget holds focus). Single-file chunks, the usual gates (no-panic + MSRV 1.75 + the lockfile pins).
6. **Cycle exit** → standard compaction: `MASTER_LOG.md`, prune any remaining remote refs on push, reset `DEV_LOG.md`; refresh this handover.
