# RamSleuth v2 — Developer Log / Lease Board

Base branch: `v2-development`. Phase 1 plan: `plans/PLAN.md`. Sign in/out under `@@@ ACTIVE_WORKERS @@@` per the lease protocol.

@@@ ACTIVE_WORKERS @@@
(no active leases)

@@@ CURRENT_STATE @@@
P1-09 merged to v2-development; ready for P1-10 (orchestrator must materialize 64B-strided tier buffer for DRAM latency).

## History
- [DONE] ID: P1-09 REVIEW | STATUS: SUCCESS | BRANCH: branch/chunk-P1-09
  DECISION: Reviewed the frozen latency kernel contract: build_chase_ring(size_bytes, seed) -> Vec<usize> (size_bytes/64 entries) and chase_latency_ns(&[usize]) -> f64 (max(ring.len(), 1_000_000) hops) match exactly; single SplitMix64-seeded Fisher-Yates permutation wired as one N-cycle (chase from 0 for N steps returns to 0, every slot visited exactly once - test (d)); reproducible per seed, different across seeds; 64-byte stride / one slot per cache line (STRIDE_BYTES=64); strictly dependent get_unchecked chase with black_box sink, no OOB (entries valid by construction + debug_asserted); serialized __rdtscp gated #[cfg(target_arch="x86_64")] with Instant self-calibration (no hardcoded TSC freq) plus documented reduced-precision Instant-only non-x86_64 fallback; // SAFETY: on every unsafe block; no bandwidth kernels in file; lib.rs wired; no new deps (no Cargo.toml change); zero clippy -D warnings (fresh re-check), 53/53 tests green debug+release, release build OK; merged --no-ff into v2-development.
  AHEAD: DESIGN NOTE FOR P1-10 (do not fail P1-09 over this): chase_latency_ns operates on the compact Vec<usize> ring (8B/entry) - the 128 MiB-config ring is 2M entries = 16 MiB, L3-resident on this 64 MiB-L3 host (~31.8 ns/hop measured = L3 latency, not DRAM). The spec 60-75 ns DRAM exit band (DDR5-6000 AM5) is only reachable once P1-10 materializes the full BufferPlan.latency_ring (128 MiB) tier buffer with slot i at physical offset 64*i and chases that 64-byte-strided buffer (working set > L3), since the frozen chase_latency_ns(&[usize]) signature operates on the compact representation; the kernel itself is verified correct as a compact/L3 measurement tool.
- [DONE] ID: P1-09 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-09
  DECISION: Implemented the pointer-chase latency kernel in latency.rs: build_chase_ring(size_bytes, seed) -> Vec<usize> builds a single-cycle SplitMix64-seeded Fisher-Yates permutation of size_bytes/64 slots (slot i = next-hop index; one full chase pass from 0 visits every slot exactly once, defeating stream/spatial prefetchers when materialized at 64-byte strides); chase_latency_ns(ring) -> f64 walks max(ring.len(), 1_000_000) strictly dependent hops (idx = ring[idx] via get_unchecked, black_box sink) timed by serialized x86_64 __rdtscp (RDTSCP waits for the full dependent chain to complete) with self-calibrating Instant ns conversion (no hardcoded TSC freq, scaling-robust) plus a documented reduced-precision Instant-only non-x86_64 fallback; // SAFETY: on the __rdtscp aux pointer and get_unchecked indexing (bounds guaranteed by construction, debug_asserted); no 128 MiB allocation in this chunk (compact index ring only; BufferPlan.latency_ring sizing is P1-03's, materialization is P1-10's); 6 new unit tests (1024-entry reproducible unique permutation, seed-dependence, finite/positive chase, single-cycle-from-0, sub-line edge sizes, build-then-chase round-trip) - 53/53 green debug and release, zero clippy -D warnings, release build OK; wired build_chase_ring + chase_latency_ns into lib.rs; informational measured ns/hop on this Ryzen 9 5950X host: ~1.3 ns (L1-resident 256-entry ring), ~5.4 ns (L2-resident 64 KiB ring), ~31.8 ns (128 MiB-config compact 16 MiB ring, L3-resident here).
  AHEAD: P1-10 orchestrator allocates the BufferPlan.latency_ring (128 MiB) buffer and materializes slot i at offset 64*i before the DRAM-tier latency pass; note the compact Vec<usize> ring is 8B/entry (16 MiB for the 128 MiB config), so chasing it directly is L3-resident on a 64 MiB-L3 host - for a true DRAM row the chased working set must exceed L3, i.e. P1-10 should chase the materialized 128 MiB buffer at 64-byte strides (a strided variant), since the frozen chase_latency_ns(&[usize]) operates on the compact representation.
- [DONE] ID: P1-08 REVIEW | STATUS: SUCCESS | BRANCH: branch/chunk-P1-08
  DECISION: Reviewed the frozen worker-dispatch contract: BenchOp/WorkerResult/WorkerError(4 variants)/run_pinned signatures match exactly; one barrier-synced worker per CpuTopology.physical_cores entry (SMT pre-filtered by P1-02) with per-CPU sched_setaffinity pinning and graceful unpinned fallback (probe fail -> single thread pinned=false, never an error); exact-once block-aligned partition (equal base chunks + remainder tail, split_at disjointness) with aggregate Read checksum == whole-buffer avx2_read/avx512_read == word-sum reference; avx2_*/avx512_* dispatch by use_avx512; // SAFETY: on all FFI + raw-pointer use; no timing, no data-buffer allocation; lib.rs wired; libc 0.2.189 the only new dep; zero clippy -D warnings; 47/47 tests green debug+release; release build OK; pinned==true on this Linux host; merged --no-ff into v2-development.
  AHEAD: P1-10 orchestrator calls run_pinned with BufferPlan-aligned buffers and owns all wall-clock timing; pinned=false is a scheduling report, not a failure; 512 path ran via the P1-07 fallback on this AVX2-only host (direct 512-body coverage remains a host caveat).
- [DONE] ID: P1-08 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-08
  DECISION: Implemented the frozen multi-threaded worker dispatch in worker.rs: run_pinned(topo, op, src, dst, use_avx512) -> Result<WorkerResult { op, total_bytes, checksum, pinned }, WorkerError>; a pre-flight sched_setaffinity probe (sched_getaffinity-read mask restored after) proves pinning works, then one std::thread::scope worker per CpuTopology.physical_cores entry (SMT already filtered by P1-02) each pins to its representative before a Barrier lockstep; the buffer is partitioned into contiguous block-aligned slices (equal base chunks, remainder tail on the last worker, split_at(_mut) disjointness) so every byte is covered exactly once; workers call avx2_*/avx512_* per use_avx512 (512 keeps P1-07's AVX2 fallback) and the joiner sums byte counters (== full buffer length) and wrapping-sums the per-slice checksums (additive over an exact word partition, so it equals the whole-buffer kernel checksum; Write carries the kernel's byte counter); pinning failure or a non-Linux platform degrades to a single unpinned thread with pinned=false (a success with a scheduling report, not an error), an empty buffer short-circuits to a zero result, and contract violations (length mismatch, 32/64 misalignment, non-block-multiple length) return WorkerError before any kernel call (no release UB); libc 0.2 is the only new dependency (affinity via an opaque 128-byte cpu-set with endianness-aware bit placement — libc 0.2.189 hides cpu_set_t's fields on glibc, so no field access); 7 new unit tests (1 MiB per-op x per-kernel-family coverage + pinned==true on Linux, Read aggregate == whole-buffer avx2_read/avx512_read, non-even-split tail, empty-topology unpinned fallback, contract errors, slice-boundary partition invariants, pre-flight mask restore) — 47/47 green debug and release, zero clippy warnings, workspace check clean.
  AHEAD: P1-10 orchestrator calls run_pinned with BufferPlan-aligned buffers and owns all wall-clock timing (none in this chunk); pinned=false is a scheduling report, not a failure; on this AVX2-only host the 512 path ran via the frozen P1-07 fallback, so direct 512-body worker runs remain unexercised here (same caveat as P1-07); libc 0.2.189's hidden cpu_set_t fields forced the opaque byte-array mask — revisit if libc ever exposes a public CpuSet API.
- [DONE] ID: P1-07 REVIEW | STATUS: SUCCESS | BRANCH: branch/chunk-P1-07
  DECISION: Reviewed the frozen avx512_read/write/copy contract: _mm512_load_si512 aligned loads + _mm512_add_epi64 accumulation with an 8-lane horizontal reduction (P1-04 word-sum checksum), unrolled _mm512_stream_si512 NT stores + a single trailing _mm_sfence() (write returns dst.len()), and the 512-bit copy (load + NT store + SFENCE, destination checksum via the frozen avx2_read); all three always callable via CpuFeatures::detect().avx512f dispatch falling back to the AVX2 kernels (no scalar reimplementation); #[target_feature(enable="avx512f")] + cfg(x86_64) gating with #[clippy::msrv = "1.89"]; 64B-aligned / len%64==0 / non-aliasing preconditions debug_asserted; // SAFETY: on every unsafe block; no timing; wired into lib.rs; checksum/return conventions exactly match the AVX2 fns; zero clippy -D warnings; 40/40 tests green debug and release; release build OK; merged --no-ff into v2-development.
  AHEAD: Host is AVX2-only (no AVX-512F): 512-bit bodies compiled and the verified AVX2 fallback branch ran with exact-equality assertions, but direct-body execution remains unexercised here; workspace MSRV 1.75 < 1.89 is a plan-level follow-up; P1-08 worker dispatch can consume avx512_read/write/copy as the 512 upgrade path.
- [DONE] ID: P1-07 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-07
  DECISION: Implemented AVX-512F read/write/copy kernels in kernel_512.rs: 512-bit aligned _mm512_load_si512 loads + _mm512_add_epi64 lane accumulation with 8-lane horizontal reduction (P1-04 word-sum checksum), unrolled _mm512_stream_si512 non-temporal stores of eight LE pattern copies + single trailing _mm_sfence() (returns dst.len()), 512-bit copy (load+NT store+SFENCE, checksum of dst via frozen avx2_read); all three always callable via CpuFeatures::detect().avx512f dispatch falling back to the AVX2 kernels (no scalar reimplementation); 64B-aligned / len%64==0 / non-aliasing preconditions debug_asserted and documented; #[target_feature(enable="avx512f")] + cfg(x86_64) gating with #[clippy::msrv = "1.89"] on each 512-bit body (AVX-512F core::arch intrinsics stabilized in Rust 1.89, newer than the workspace MSRV 1.75); wired into lib.rs; 5 unit tests covering all gates (8 KiB checksum parity vs avx2_read + word-sum reference, pattern fill + byte count, copy byte-identity + checksum parity vs avx2_copy/copy_from_slice, minimal 64-B buffers, direct-body run or verified AVX2 fallback); 40/40 green debug and release, clippy -D warnings clean, release build OK on this AVX2-only (no AVX-512F) host.
  AHEAD: Reviewer: verify the 512-bit body shapes on an AVX-512F host (direct-body test branch (e) did not execute here); workspace MSRV 1.75 < 1.89 required to compile this module on x86_64 is a plan-level follow-up decision (out of this chunk's file scope); P1-08 worker dispatch can consume avx512_read/write/copy as the 512 upgrade path.
- [DONE] ID: P1-06 REVIEW | STATUS: SUCCESS | BRANCH: branch/chunk-P1-06
  DECISION: Reviewed frozen avx2_copy contract: unrolled 4-wide aligned _mm256_load_si256(src) + _mm256_stream_si256(dst) with tail, single trailing _mm_sfence(), #[target_feature(enable=avx2)] + cfg(x86_64) gating, CpuFeatures-dispatched copy_from_slice scalar fallback, // SAFETY: on every unsafe block, non-aliasing precondition documented + debug_asserted, no timing, module wired in lib.rs, P1-04 avx2_read word-sum checksum reused; zero clippy warnings; 35/35 tests green debug and release; SIMD body genuinely exercised on this AVX2 host; destination byte-identical to a direct copy_from_slice; merged --no-ff into v2-development.
  AHEAD: P1-07 512-bit variants reuse the (src,dst,len) helper + word-sum checksum convention; P1-08 worker dispatch consumes avx2_copy.
- [DONE] ID: P1-06 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-06
  DECISION: Implemented AVX2 copy kernel: unrolled 4-wide _mm256_load_si256(src) + _mm256_stream_si256(dst) interleaved with tail, single _mm_sfence(); returns P1-04 word-sum checksum of dst via frozen avx2_read (data-sensitive, DCE-defeating); preconditions 32B-aligned both sides, equal 32-multiple length, non-aliasing (debug_asserted, documented); CpuFeatures-dispatched copy_from_slice scalar fallback; 4 unit tests (byte-identical 4 KiB pattern + checksum vs word_sum/direct copy_from_slice, stable/data-sensitive checksum, 32-B minimum, scalar-vs-SIMD identical destination); 35/35 green debug+release on AVX2 host, zero clippy warnings.
  AHEAD: Reviewer: verify NT-store + SFENCE tail shape, non-aliasing precondition, and avx2_read checksum reuse on host; P1-07 512-bit copy reuses the (src,dst,len) helper + checksum convention; P1-08 worker dispatch consumes avx2_copy.
- [DONE] ID: P1-05 REVIEW | STATUS: SUCCESS | BRANCH: branch/chunk-P1-05
  DECISION: Reviewed frozen avx2_write contract: NT stores only via _mm256_stream_si256, single trailing _mm_sfence(), AVX2 + cfg gating, scalar fallback with identical contents, SAFETY comments on every unsafe block, no timing, lib.rs wiring, P1-04 pattern reuse; clippy -D warnings zero; 31/31 tests green debug and release; SIMD path exercised on AVX2 host; merged --no-ff into v2-development.
  AHEAD: P1-06 copy kernel reuses the (ptr, len) + _mm256_stream_si256 + _mm_sfence tail shape; AVX2 host confirmed.
- [DONE] ID: P1-05 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-05
  DECISION: Implemented AVX2 non-temporal write kernel: _mm256_set_epi64x pattern vector + _mm256_stream_si256 unrolled 4-wide + tail, single _mm_sfence() after the last store; returns bytes written (dst.len()); CpuFeatures-dispatched safe scalar fallback (LE u64 words, identical contents); 4 unit tests (pattern-filled 4 KiB + spot-check, stable/non-zero return, 32-B minimum, scalar-vs-SIMD contents), 31/31 green debug+release on AVX2 host, zero clippy.
  AHEAD: Reviewer: verify NT-store + SFENCE shape on host; P1-06 copy kernel reuses the (ptr,len) + _mm256_stream_si256 + _mm_sfence tail pattern; PLAN.md P1-05 "returns bytes written" signature is the frozen one.
- [DONE] ID: P1-04 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-04
  DECISION: Implemented AVX2 streaming read kernel: _mm256_load_si256 unrolled 4-wide + tail, _mm256_add_epi64 lane accumulation, horizontal u64 reduction; checksum = wrapping sum of LE u64 words (identical SIMD/scalar, 0 for zeros); CpuFeatures-dispatched safe scalar fallback; 4 unit tests (zeroed 4 KiB, stability/DCE, 32-B minimum, scalar fallback), 27/27 green debug+release, zero clippy.
  AHEAD: Reviewer: verify thin-pointer simd helper shape on host; P1-05/P1-06/P1-07 must reuse the word-sum checksum definition + (ptr,len) helper pattern; PLAN.md P1-04 "returns bytes processed" text is superseded by the checksum signature.
- [DONE] ID: P1-02 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-02
  DECISION: Reviewed frozen detect() -> Result<CpuTopology, TopologyError>; merged after zero clippy warnings and 13/13 tests green.
  AHEAD: P1-03/P1-08 must consume CpuTopology fields (physical_cores, total_l3_bytes, ccd_l3_bytes); PLAN.md P1-02 text is stale (discover/PhysicalCore).
- [DONE] ID: P1-03 | STATUS: SUCCESS | BRANCH: branch/chunk-P1-03
  DECISION: Reviewed pure plan(&CpuTopology) -> BufferPlan (L1/L2 sysfs+safe fallbacks, per-CCD L3 slice, DRAM max(256 MiB, 3x total L3), 128 MiB ring, all 64-B aligned); merged after zero clippy warnings and 23/23 tests green.
  AHEAD: P1-04/P1-08/P1-09 consume BufferPlan fields (l1,l2,l3,dram,latency_ring); PLAN.md P1-03 text is stale (BufferProfiles/size_for, 16 KB/256 KB).
