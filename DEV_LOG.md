# RamSleuth v2 — Developer Log / Lease Board

Base branch: `v2-development`. Plan: `plans/PLAN-PHASE3.md` (Phase 1 retained as `plans/PLAN.md`, Phase 2 as `plans/PLAN-PHASE2.md`). Sign in/out under `@@@ ACTIVE_WORKERS @@@` per the lease protocol. Durable cycle history lives in `MASTER_LOG.md`.

@@@ ACTIVE_WORKERS @@@
(no active leases)

@@@ CURRENT_STATE @@@
Cycle 3 (Phase 3) - P3-18..P3-21 ramsleuth-client (P3-18 transport, P3-19 dump renderer, P3-20 bench/status, P3-21 CLI) MERGED into v2-development via --no-ff (e9082af; LOCAL ONLY, never pushed). Scope audit: the diff touches ONLY crates/ramsleuth-client/** (Cargo.toml, src/lib.rs, src/client.rs, src/dump.rs, src/commands.rs, src/main.rs), Cargo.lock, DEV_LOG.md - no other file. No-panic contract verified by audit + tests: missing socket -> friendly DaemonDown after 3 retries (100+200 ms backoff) naming the path + the start hint, protocol violations -> ClientError::Protocol, closed stream mid-frame -> Io(UnexpectedEof), silent daemon -> ClientError::Timeout; the only production expect (client.rs:247) is provably unreachable (the retry loop only exits via early returns after recording every transient failure). Workspace 260/260 (16 suites, 0 failed), clippy --workspace --all-targets -D warnings clean, build green. **CORE GATE (Phase 3 exit criterion) PASSED** on this host (unprivileged user, AMD Ryzen 9 5950X, no ryzen_smu driver), daemon on /tmp/ramsleuth-core.sock (mode 0660, 'ramsleuth-daemon listening on ...' line printed): (1) `dump` exit 0 with the full dashboard - CPU live (AMD Zen3 / Ryzen 9 5950X 16-Core), AMD `N/A (driver missing)` (EXPECTED graceful degradation without ryzen_smu), Intel `N/A (unsupported hardware)`, SPD 2 live DDR4 modules (rank 1, 3200 MT/s, per-cell N/A with reasons) - every cell a value or N/A (<reason>), no panic; (2) `status` exit 0 - CPU: ok / AMD: N/A (DriverMissing) / Intel: N/A (UnsupportedHardware) / SPD: ok (2 modules) + daemon socket line; (3) `bench --tier memory` exit 0 - 'benchmark started (run 1)' + 3 progress lines + terminal 4x4 grid with live values (Memory 26.35/43.63/12.11 GB/s + 86.84 ns/hop; other tiers N/A); (4) daemon SIGTERM -> 'received SIGTERM, shutting down', exit 0, socket file removed (verified twice: tracked-process stop + a fresh `timeout --preserve-status --signal=TERM` cycle on /tmp/ramsleuth-core2.sock); (5) NEGATIVE path with no daemon (`--socket /tmp/ramsleuth-nosuch.sock dump`) -> exit 1 with the friendly DaemonDown hint, no panic. All temp sockets/files cleaned up. Phase 3 exit criterion satisfied. NEXT: P3-22 (TUI, first TUI/GUI chunk) may begin - it consumes the ramsleuth_client lib (Client + ClientError + dump::render + commands::{bench, status, render_grid}) over the same 0660 socket, keeping the DaemonDown hint + USAGE block stable.
@@@ HISTORY @@@
- [DONE] ID: P3-18..P3-21-Review | STATUS: SUCCESS | BRANCH: v2-development
DECISION: code review + merge of the full unprivileged client chain (branch/chunk-P3-21 = P3-18 transport / P3-19 dump renderer / P3-20 bench+status / P3-21 CLI) into v2-development via --no-ff (e9082af, local only, never pushed): scope audit passed (the diff touches ONLY crates/ramsleuth-client/** (Cargo.toml, lib.rs, client.rs, dump.rs, commands.rs, main.rs), Cargo.lock, DEV_LOG.md), no-panic contract audited + test-covered (missing socket -> DaemonDown after 3 retries with the start hint; Protocol on frame violations + unexpected arms; Io(UnexpectedEof) on a closed stream; Timeout on a silent daemon; the only production expect, client.rs:247, is provably unreachable), workspace 260/260 (16 suites, 0 failed), clippy --workspace --all-targets -D warnings clean, build green; CORE GATE (Phase 3 exit criterion) passed unprivileged on this AMD 5950X host against /tmp/ramsleuth-core.sock (mode 0660, listening line printed): dump exit 0 with the full dashboard (CPU live AMD Zen3 / Ryzen 9 5950X 16-Core, AMD N/A (driver missing) - expected without ryzen_smu, Intel N/A (unsupported hardware), 2 live SPD DDR4 modules at 3200 MT/s with per-cell N/A reasons), status exit 0 (CPU: ok / AMD: N/A (DriverMissing) / Intel: N/A (UnsupportedHardware) / SPD: ok (2 modules) + socket line), bench --tier memory exit 0 (BenchStarted run 1 + 3 progress lines + 4x4 grid, live 26.35/43.63/12.11 GB/s + 86.84 ns/hop), daemon SIGTERM -> graceful line + exit 0 + socket removed (verified on a second fresh cycle), negative path with no daemon -> exit 1 + friendly DaemonDown hint, no panic; temp files cleaned.
AHEAD: Phase 3 exit criterion satisfied - P3-22..P3-30 (TUI/GUI) may begin: the TUI consumes the ramsleuth_client lib (Client + ClientError + dump::render + commands::{bench, status, render_grid}) over the same 0660 socket; the DaemonDown hint text and the USAGE block are stable user-facing strings; per-subcommand read timeouts (10 s fast / 120 s between bench frames) ride on the shared transport.
- [DONE] ID: P3-21 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-21
DECISION: rewrote crates/ramsleuth-client/src/main.rs from the stub (P3-21 CLI entry point, exactly this one file): pure `parse_cli<I: IntoIterator<Item=S>, S: AsRef<str>>(args) -> Result<CliArgs, String>` (no env access — first positional is the subcommand `dump`|`bench`|`status` defaulting to `dump` when absent; `--socket <path>` defaulting to the protocol's `DEFAULT_SOCKET_PATH`; `--tier memory|l1|l2|l3|full` mapping onto `StreamTarget::Tier(..)`/`StreamTarget::Full` (default Full); `--mode full|memory-only` mapping onto `BenchMode::Full`/`MemoryOnly` (default Full); flags repeatable last-wins, any order relative to the subcommand; unknown flags/subcommands, a second positional, missing values, and bad enum values are `String` errors naming the offending token) + `CliArgs { socket, subcommand, target, mode }` (Debug/Clone/PartialEq + manual Default) + `Subcommand { Dump, Bench, Status }` (Debug/Clone/Copy/PartialEq) + `const USAGE` (the ramsleuth-daemon P3-17 precedent; the frozen `DEFAULT_SOCKET_PATH` as a literal) + `main()`: parse (error + usage text -> exit 2) -> `Client::connect` (error -> the friendly `DaemonDown` "start it with ..." hint -> exit 1) -> per-subcommand read timeout (10 s dump/status, 120 s between bench frames; the transport default is 5 s) -> dispatch `dump::dump` (P3-19, the exit-criterion command) / `commands::bench(&mut client, target, mode)` (P3-20; the returned printed text is ignored via `.map(drop)`) / `commands::status` (P3-20); Ok -> exit 0, any `ClientError` -> exit 1. 14 `parse_cli` unit tests: the brief's cases (a)-(j) verbatim (no-args defaults incl. `socket == DEFAULT_SOCKET_PATH`, `dump --socket /tmp/x.sock`, `bench --tier memory --mode memory-only` -> `Tier(Memory)`/`MemoryOnly`, `status`, `bench --tier full` -> `Full`, unknown subcommand `frobnicate` -> Err, `--tier` no value -> Err, `--tier bogus` -> Err, `--mode bogus` -> Err, unknown flag `--bogus` -> Err) plus all five tier spellings, a second positional, missing values for all three flags, and owned-`String` args (the generic bound, the daemon test precedent). ramsleuth-client 35/35 (21 lib + 14 bin), workspace clippy `--all-targets -D warnings` clean, workspace build green, exit codes smoke-verified live (usage -> 2 with the usage text, missing socket -> 1 with the daemon-down hint), MSRV 1.75-safe (no post-1.75 APIs; only std + the three path deps). Local branch only: NOT pushed, NOT merged.
AHEAD: the CORE GATE is the next step (reviewer, same unprivileged user): `cargo run -p ramsleuth-daemon -- --socket /tmp/ramsleuth.sock` then `cargo run -p ramsleuth-client -- --socket /tmp/ramsleuth.sock dump` must print the full hardware timings (exit 0) — then `... bench --tier memory --mode full` and `... status`; only after the gate clears may P3-22..P3-30 (TUI/GUI) begin. The `DaemonDown` hint text and the USAGE block are stable user-facing strings (P3-18 contract).
- [DONE] ID: P3-20 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-20
DECISION: built commands.rs — the P3-20 bench + status commands over the shared transport: `pub fn render_grid(grid: &BenchmarkGrid) -> String` (pure: the AIDA64-style 4x4 table, tier rows x Read/Write/Copy/Latency columns, GB/s + ns/hop per the Phase 1 convention, every line equal-width (column pads to the wider of its header and its four cells), an unmeasured cell — 0.0 on the wire, the grid carries no Na arm — prints N/A); `pub fn bench(client: &mut Client, target: StreamTarget, mode: BenchMode) -> Result<String, ClientError>` = one StartBenchmark send, then a recv loop over the daemon's reply stream (P3-11 incremental contract) printing each event live: BenchStarted -> "benchmark started (run N)", BenchProgress -> "  [cell/total] tier/op: value GB/s", BenchResult -> render_grid, BenchCancelled -> "benchmark cancelled (run N)" (clean Ok), Error(msg) -> ClientError::Protocol(msg), other -> Protocol("unexpected response during benchmark"); `pub fn status(client: &mut Client) -> Result<String, ClientError>` = one GetTelemetry round trip -> one line per major section (CPU: ok by construction; AMD/Intel: ok when the branch holds a Value, else N/A (<reason>) via the NaReason Debug form; SPD: ok (n modules) / N/A (no modules)) + the daemon socket line; both commands print AND return the same String (documented deviation from the brief's Result<(), ClientError> sketch, per its own testability license — in-process stdout capture is unreliable: std dups fd 1 on first use; the P3-21 bin ignores the return value); lib.rs: `pub mod commands` + root re-export `commands::{bench, render_grid, status}` + module doc list. 8 unit tests on the in-process daemon stand-in (pid-qualified temp socket, request-assert + scripted reply frames): bench started+progress+grid (returned text carries every line, no N/A on a full grid), bench cancel terminal Ok, bench structured Error -> Protocol verbatim, status mixed snapshot (AMD Na + Intel Value via decode_channel + 1 SPD module + socket line), status all-Na snapshot, status structured Error -> Protocol, render_grid full (equal-width lines, units, no N/A), render_grid partial (15 N/A + aligned). client 21/21, workspace 246/246, clippy --workspace --all-targets -D warnings clean, build green, MSRV 1.75-safe.
AHEAD: P3-21 (main) dispatches on a connected Client: `dump(&mut client)` (P3-19), `bench(&mut client, target, mode)` + `status(&mut client)` (this chunk — both print and return their text; StreamTarget/BenchMode come from ramsleuth_protocol, the --tier/--mode CLI maps onto them), `render_grid` for any grid export; the progress/cancel/socket-line texts and DaemonDown hint are stable user-facing strings.
- [DONE] ID: P3-19 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-19
DECISION: built dump.rs — the P3-19 dashboard renderer + the exit-criterion `dump` command: `pub fn render(telemetry: &SystemMemoryTelemetry) -> String` (pure: no I/O, no client, deterministic — walks CPU/AMD/Intel/SPD, every Section cell renders its formatted value (MHz/Ω/RTT/V/MT/s/ticks/on-off) or `N/A (<reason>)` via a renderer-owned human-readable NaReason text (NaReason has no Display); all four section headers always print, an absent branch prints one N/A line, empty SPD a no-modules note — the all-Na driver-less state is a complete dashboard, never a panic) with AMD in clocks-ratios / primary / secondary / tertiary+turnarounds / CAD-bus / voltages subgroups (27 timings canonical order) and Intel per-channel (+RTL) reusing the same row builders; `pub fn dump(client: &mut Client) -> Result<(), ClientError>` = one GetTelemetry round trip (Telemetry -> println(render) + Ok, Error(msg) -> ClientError::Protocol(msg), other -> Protocol("expected telemetry")); lib.rs gains `pub mod dump` + root re-export `dump::{dump, render}` (module + function coexist in separate namespaces); 7 unit tests on public-API fixtures (representative AMD + 2-module SPD / all-Na / Intel 2-channel via decode_channel / determinism / header presence / dump stand-in round trip + Error mapping, pid-qualified temp socket, join-after-connect pattern); client 13/13, workspace 238/238, clippy --workspace --all-targets -D warnings clean, build green, MSRV 1.75-safe.
AHEAD: P3-21 (main) calls `Client::connect(socket_path)` then `dump::dump(&mut client)` (or `dump()` via the root re-export) and `render` for any export; P3-20 (commands.rs) is independent of dump but shares the same `Client`; P3-24 (TUI snapshot) reuses the pure `render`. CORE GATE still needs the P3-21 `--socket` plumbing before the live `client -- dump` run.
- [DONE] ID: P3-18 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-18
DECISION: made ramsleuth-client a lib+bin crate (explicit [lib] ramsleuth_client + [[bin]]; deps = the three path deps protocol/telemetry/bench only — no tokio, no serde, D2) + client.rs transport: Client { stream: UnixStream, socket_path, leftover buf } (Debug, private fields, socket_path() accessor) + ClientError { Connect(io), Timeout, Protocol(String), Io(io), DaemonDown(String) } (Debug derived; Clone/PartialEq hand-implemented on the io arms via kind+text — the daemon RpcError precedent; Display + Error with source() on the io arms); connect = UnixStream::connect (documented deviation: std UnixStream has no connect_timeout — a local connect cannot hang, ENOENT/ECONNREFUSED are instant) with RETRIES=3 and 100 ms·(n+1) backoff on NotFound/ConnectionRefused -> DaemonDown("cannot connect to <path>: daemon not running? start it with `cargo run -p ramsleuth-daemon` or `systemctl start ramsleuth` (last error: ...)"), other io -> Connect; 5 s default read/write timeouts set on connect + set_read_timeout/set_write_timeout overrides (documented std quirk: an expired UnixStream read timeout surfaces as WouldBlock, not TimedOut — both map to Timeout); send = encode_frame(Message::Request(*req)) + write_all (FrameError -> Protocol, io -> Io); recv = persistent growable buffer + 16 KiB chunks, decode_frame loop (Incomplete -> read more, Oversized/Decode -> Protocol, 0-byte read -> Io(UnexpectedEof), a request frame where a response is due -> Protocol("unexpected request frame") — leftover bytes survive across recv for streaming, P3-11 contract); request = send + recv (one round trip); lib.rs root re-export of {Client, ClientError} (workspace style) + main.rs stub doc updated for P3-21; 6 unit tests on a REAL in-process daemon stand-in (spawned thread binds a UnixListener on a pid-qualified temp socket, joined so a panic fails the test): (a) connect to the live socket ok + socket_path accessor, (b) request(&GetTelemetry) -> the stand-in's canned Response::Telemetry (stand-in decodes the request frame, answers an encoded Telemetry frame), (c) non-existent path -> friendly DaemonDown naming the path after the observed 100+200 ms backoff floor (no panic, no hang), (d) recv on a closed stream -> Io(UnexpectedEof), (e) silent daemon -> Timeout (200 ms read timeout), (f) stand-in echoing a request frame -> Protocol; client 6/6, workspace 231/231, clippy -D warnings clean (clone-on-copy, doc-list-item, unused-mut fixed), build green, MSRV 1.75-safe.
AHEAD: P3-19 (dump) = Client::connect(&path) + request(&GetTelemetry) -> Response::Telemetry(SystemMemoryTelemetry) -> render (pure formatting); P3-20 (bench/status) reuses the same Client — request() is single-round-trip only (GetTelemetry/CancelBenchmark); streamed bench progress rides the leftover-buf recv loop (BenchStarted -> BenchProgress... -> one terminal), the P3-16 daemon owner pump is the peer; P3-21's bin is the main.rs placeholder. DaemonDown's message text is user-facing — keep it stable across P3-19..P3-21.
- [DONE] ID: P3-12..P3-17-merge | STATUS: SUCCESS | BRANCH: v2-development
DECISION: code review + merge of the full daemon chain (branch/chunk-P3-17) into v2-development via --no-ff: scope audit passed (diff touches only crates/ramsleuth-daemon/**, systemd/ramsleuth.service, Cargo.lock, DEV_LOG.md), safety-contract audit passed (caps probe never panics/exits; socket stale probe handles live -> AlreadyRunning and stale -> rebind without panicking; RPC dispatch closes the connection on Oversized/Decode frames; JobManager cancel path is clean with no leaked threads), workspace 225/225, clippy -D warnings clean, build green, live smoke test passed (listening line, socket mode 0660, SIGTERM -> exit 0, socket removed), post-merge tree clean.
AHEAD: P3-18 (client crate birth) forks from the current v2-development tip e269693; the client connects to the daemon socket (default DEFAULT_SOCKET_PATH, mode 0660) — a live daemon is simply connected to, never re-bound (AlreadyRunning is a daemon-side guard).
- [DONE] ID: P3-17 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-17
DECISION: rewrote the daemon bin (main.rs replaces the stub) + added systemd/ramsleuth.service: pure parse_args over --socket <path> (default DEFAULT_SOCKET_PATH) and --max-age <secs> (non-negative integer seconds, default 2 s; repeatable, last wins; missing value / negative / non-numeric / unknown arg -> Err naming the problem; nanosecond-overflow guard before Duration::from_secs) + DaemonArgs (Debug/Clone/PartialEq + manual Default) + #[tokio::main] no-panic flow (D5): --help usage + exit 0, parse error + usage exit 2, probe() warnings soft to stderr, one DaemonContext{cache: Arc<Mutex<TelemetryCache::new(ramsleuth_telemetry::collect, max_age)> (clippy: pass the fn, not a closure), jobs: Arc<BenchJobManager::new()>}, setup_listener inside the runtime (fatal -> exit 1), listening line to stdout, SIGTERM/SIGINT via tokio::signal::unix Signal::recv() in select! -> break, best-effort socket remove (warning only) -> exit 0; 9 parse_args tests ((a)-(g) per the brief + last-wins + owned String) + live smoke run (bind, soft non-root warning, SIGTERM graceful, socket removed, exit 0); the unit: root + ONLY CAP_SYS_RAWIO (bounding + ambient), NoNewPrivileges, ProtectSystem=strict/ProtectHome/PrivateTmp, RuntimeDirectory+ReadWritePaths=/run/ramsleuth, Restart=on-failure, WantedBy=multi-user.target, header = packaging artifact (Phase 5) + ryzen_smu module note; daemon 34/34, workspace clippy -D warnings clean, build green, MSRV 1.75-safe.
AHEAD: CORE GATE live test launches the daemon with `cargo run -p ramsleuth-daemon -- --socket /tmp/ramsleuth.sock` (unprivileged: expect the soft warnings, GetTelemetry serves with N/A privileged fields; SIGTERM/SIGINT exit 0 + remove the socket) — the P3-18 client connects to exactly that socket path; a root run without CAP_SYS_RAWIO degrades the same way (warning only).
- [DONE] ID: P3-16 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-16
DECISION: built rpc.rs — the daemon's per-connection async RPC loop over the frozen P3-10/P3-11 wire contract: shared DaemonContext{cache: Arc<Mutex<TelemetryCache>>, jobs: Arc<BenchJobManager>} (the per-daemon state P3-17 hands each connection) + RpcError{Io, Protocol, Job, Join} (hand Clone/PartialEq on the io::Error arm by kind+text — P3-13 precedent; Display + std::error::Error) + handle_connection(stream: tokio UnixStream, ctx: Arc<DaemonContext>) -> Result<(), RpcError> (into_split; 16 KiB reads -> growable buffer -> decode_frame: Incomplete reads more, Oversized/Decode close with Protocol, EOF returns Ok(())) with dispatch: GetTelemetry served off the blocking pool (mutex + poison recovery, never a parked worker) -> Response::Telemetry; StartBenchmark -> jobs.start(*target, *mode, 0=auto) (JobError -> Job arm, Busy keeps its "benchmark already running" wire text) -> BenchStarted{run_id} + owner pump forwarding every JobEvent via spawn_blocking(std mpsc recv) — Progress -> BenchProgress in order, exactly one terminal -> BenchResult{run_id, grid} / BenchCancelled{run_id} / Error, channel close breaks (defensive); CancelBenchmark -> jobs.cancel -> BenchCancelled{run_id} ack or Error("no active run with id N") from any connection (D6); client-sent Message::Response -> Protocol("unexpected response frame") + close; write_frame = encode_frame + write_all + flush on &mut (impl AsyncWrite + Unpin) (+Unpin compiler-forced, OwnedWrite satisfies it); lib.rs + `pub mod rpc` + root re-export of {DaemonContext, handle_connection, RpcError} (workspace style); 7 tests on UnixStream::pair() (in-memory, no socket file) with a MOCK ctx (fixed all-Na snapshot collector + real BenchJobManager): telemetry serve + clean EOF -> Ok, minimal single-cell L1 Read run -> BenchStarted -> 1 Progress (value > 0) -> BenchResult (grid cell > 0), cross-connection cancel -> BenchCancelled/Error ack + owner still gets exactly one terminal, forged u32::MAX prefix -> Protocol (size guard), corrupt 0xFF variant byte -> Protocol (decode), client Response frame -> Protocol (unexpected), RpcError trait impls; daemon 25/25 (~4 s), workspace 216/216, clippy -D warnings clean, build green.
AHEAD: P3-17 (main) must build one DaemonContext{cache: Arc::new(Mutex::new(TelemetryCache::new(ramsleuth_telemetry::collect, max_age))), jobs: Arc::new(BenchJobManager::new())} at startup and, per accepted connection from setup_listener, tokio::spawn(handle_connection(stream, ctx.clone())) — Ok(()) = clean disconnect, RpcError = logged close; nothing shared is torn down by a dying owner (the run releases its own slot, D6).
- [DONE] ID: P3-15 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-15
DECISION: built bench_job.rs — the daemon's single-flight benchmark core over the frozen P3-09 contract: BenchJobManager (Arc-backed busy/next_run_id/active; Clone; the spawned job clears active — only if it still holds this run_id — and drops busy itself, so the manager is borrow-free and re-entrant) + JobHandle{run_id, std mpsc::Receiver<JobEvent>} + JobEvent{Progress(StreamProgress), Result(BenchmarkGrid), Cancelled, Error(String)} + JobError{Busy, Spawn(String)} (Display + std::error::Error, Default for clippy); async start folds (target, mode) onto the effective StreamTarget (Full: as-is; MemoryOnly: clamped to the memory tier — Cell keeps its op) and runs spawn_blocking(run_streamed) with the P3-09 progress channel FIFO-pumped into the owner's JobEvent stream (terminal always last, channel closes after it); cancel(run_id) sets the flag (Relaxed) -> true iff active; lib.rs re-exports at the root (workspace style); 6 unit tests (multi_thread 2-worker + 30 s close-guard: single-cell Progress+Result, Busy while running, re-start + monotonic ids, cancel -> Cancelled terminal + false before/after, mapping table, error Display/Error); daemon 18/18, workspace tests all green, clippy -D warnings clean, build green.
AHEAD: P3-16 (rpc) calls BenchJobManager::start(target, mode, threads).await (Busy -> wire Response::Error("benchmark already running")), replies BenchStarted{handle.run_id}, drains JobEvent (Progress -> BenchProgress; Result -> BenchResult; Cancelled -> BenchCancelled; Error -> Error) until the channel closes, and services CancelBenchmark{run_id} via cancel(run_id); JobHandle.events is a std mpsc Receiver (not tokio) — pump it off the async context (e.g. spawn_blocking) to avoid blocking a runtime worker.
- [DONE] ID: P3-01-Review | STATUS: SUCCESS | BRANCH: branch/chunk-P3-01
DECISION: Merged P3-01 (serde derives on NaReason + Section<T>) after clean scope/test/clippy/build audit; committed worker Cargo.lock (serde 1.0.229 + bincode 1.3.3) as 34543f8.
AHEAD: P3-02 may consume the NaReason/Section serde derives; runtime bincode stays owned by ramsleuth-protocol (P3-10).
- [DONE] ID: P3-04 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-04
DECISION: serde derives on IntelChannel + IntelReadout (the only pub structs in intel_readout.rs) plus a bincode round-trip test (populated + all-Na multi-channel); 96/96 tests, clippy clean, workspace build green.
AHEAD: P3-05 (spd_decode) is independent; P3-06's Section<IntelReadout> needs P3-04 merged first.
- [DONE] ID: P3-05 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-05
DECISION: serde derives on SpdProfile + SpdModule (the only pub structs in spd_decode.rs; the JEP106 const and all decode logic untouched) plus a bincode round-trip test (representative SpdModule, 2-module DDR4+DDR5 fixture, all-Na with empty profiles); 97/97 lib + 6/6 bin tests, clippy clean, workspace build green. Pending P3-04 sign-out was first committed on branch/chunk-P3-04 (d7c07ee) for a clean base.
AHEAD: P3-06 (facade) can now derive SystemMemoryTelemetry - the spd section is wire-ready; maker 0xC1 / density 0x0D still pending live reconciliation (no decode logic changed here).

- [DONE] ID: P3-06 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-06
DECISION: serde derive + PartialEq on SystemMemoryTelemetry (the payload root) in facade.rs only - all field types already serde-derived by P3-01..P3-05 - plus whole-snapshot bincode round-trip tests (representative Value+Na mix across CPU/AMD/Intel/SPD, all-Na, live collect()); 99/99 lib + 6/6 bin tests, clippy clean, workspace build green. Pending P3-05 sign-out was first committed on branch/chunk-P3-05 (d533e6e) for a clean base.
AHEAD: telemetry serde foundation is complete - P3-07..P3-09 (bench) are now independent; P3-10's Response::Telemetry payload root is wire-ready.
- [DONE] ID: P3-07 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-07
DECISION: serde derives on BenchOp + WorkerResult (the two wire-crossing pub types in worker.rs; run_pinned + WorkerError untouched - WorkerError never crosses the wire) plus bincode round-trip tests (all 3 op arms, representative results incl. the write checksum==total_bytes invariant); 65/65 tests, clippy clean, workspace build green; bincode pinned 1.3 per the P3-01 precedent + ledger (resolves to the same locked 1.3.3).
AHEAD: WorkerResult is wire-ready for the protocol payload inventory (P3-10); P3-08 (orchestrator serde + pub(crate) widening) is independent of this chunk.
- [DONE] ID: P3-08 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-08
DECISION: serde derives on Tier/Metric/BenchmarkGrid (the wire-crossing pub types in orchestrator.rs) + bincode round-trip test (f64 arrays bit-exact, enum arms serde-ready) + five pass helpers widened from private to pub(crate) - AlignedBuf, normalize_size, fill_pattern, bench_bandwidth, bench_latency - for P3-09's run_streamed reuse; no signature edits, materialize_chase/chase_materialized/run_all_sized/run_all untouched; 66/66 tests (8 orchestrator), clippy -D warnings clean, workspace build green.
AHEAD: P3-09 (streamed.rs) can reuse the widened passes; BenchmarkGrid is wire-ready for the protocol payload inventory (P3-10).
- [DONE] ID: P3-09 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-09
DECISION: built streamed.rs - StreamTarget/StreamOptions/StreamProgress/StreamError + run_streamed (clean relaxed cancel before start and between cells, per-cell progress via .ok(), P3-08 pass reuse, host-resolved topology/kernel/threads) + 7 tests; two documented scope deviations forced by the frozen contract: WorkerError serde (Serialize derive + hand-written Deserialize via String mirror for the &'static str field) and AlignedBuf method pub(crate) widening; Cargo.toml comment corrected; 73/73 bench tests, clippy -D warnings clean, workspace build green.
AHEAD: P3-15 must call ramsleuth_bench::run_streamed(&StreamOptions { target, threads, cancel: Arc<AtomicBool> }, Option<mpsc::Sender<StreamProgress>>) -> Result<BenchmarkGrid, StreamError>; StreamProgress/StreamError are bincode-ready and WorkerError is now wire-serializable (P3-10 payload audit should note it).
- [DONE] ID: P3-06/09-Review | STATUS: SUCCESS | BRANCH: v2-development
DECISION: Reviewed + merged both foundation chains (A: c5c745c telemetry serde; B: bd6115a bench serde + streamed) after scope/test/clippy/build audits; resolved the DEV_LOG.md merge conflict as the union of P3-04..P3-09 history entries; post-merge tree 178/178 green, clippy -D warnings clean, build clean.
AHEAD: P3-10 (protocol) builds on the wire-ready payload roots: SystemMemoryTelemetry (facade), WorkerResult/BenchOp (worker), Tier/Metric/BenchmarkGrid (orchestrator), StreamProgress/StreamError (streamed); WorkerError is now wire-serializable.
- [DONE] ID: P3-10 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-10
DECISION: born ramsleuth-protocol - BenchMode{Full,MemoryOnly}/Request{GetTelemetry,StartBenchmark{target:StreamTarget,mode},CancelBenchmark{run_id}}/Response{Telemetry,BenchStarted,BenchProgress,BenchResult{run_id,grid},BenchCancelled,Error}/Message (all serde-derived, payloads reused verbatim from telemetry+bench; justified clippy::large_enum_variant allows on Response/Message - boxing would break the frozen arm shapes) + DEFAULT_SOCKET_PATH; root Cargo.toml member + [workspace.dependencies] serde/bincode/tokio; two contract-forced bench addenda (recipe scope was 4 files): StreamTarget serde derive (streamed.rs) + BenchmarkGrid PartialEq (orchestrator.rs); 5/5 protocol tests, workspace 183/183, clippy -D warnings clean, build green.
AHEAD: P3-11 (frame codec) imports ramsleuth_protocol::{Message, Request, Response, BenchMode, DEFAULT_SOCKET_PATH} exactly as declared; P3-12/15/16 construct the unboxed arm shapes (Response::Telemetry(SystemMemoryTelemetry), BenchResult{run_id, grid: BenchmarkGrid}) - any boxing now is a frozen-contract change.
- [DONE] ID: P3-11 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-11
DECISION: built frame.rs — synchronous length-prefixed Bincode codec: encode_frame(&Message) -> Vec<u8> (4-byte LE length of the bincode payload, pre-checked <= 16 MiB) + decode_frame(&[u8]) -> Frame{message, consumed} (Incomplete when <4 bytes or payload not yet fully buffered; Oversized for any declared length > MAX_FRAME_SIZE, checked before the completeness test; Decode(bincode text) on corrupt payloads) with Display+Error on FrameError; lib.rs gained `pub mod frame` + crate-root re-exports; 8 unit tests covering round-trip, exact [LE-length][payload] layout, both truncation classes, hostile 0xFFFFFFFF length, cap-boundary (length == MAX -> Incomplete), corrupt payload, two back-to-back frames with exact consumed accounting; protocol 13/13, workspace 191/191, clippy -D warnings clean, build green; zero unsafe, crate stays tokio-free.
AHEAD: P3-16 (daemon rpc) + P3-18 (client) must call ramsleuth_protocol::{encode_frame, decode_frame, Frame, FrameError, MAX_FRAME_SIZE} exactly as frozen — append bytes to a buffer, loop decode_frame until Incomplete, advance by Frame.consumed, and treat Oversized/Decode as Response::Error + close.
- [DONE] ID: P3-12 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-12
DECISION: born ramsleuth-daemon as lib+bin (explicit [lib] ramsleuth_daemon + [[bin]] ramsleuth-daemon; tokio 1.53.1 (MSRV 1.71, ledger D7), libc 0.2, protocol/telemetry/bench path deps; stub main.rs untouched, still compiles and runs exit 0) + caps.rs — the SOFT privilege probe: PrivilegeReport{is_root, has_cap_sys_rawio, warnings} (Debug/Clone/PartialEq) built from geteuid + /proc/self/status CapEff bit 21 via the pure cap_sys_rawio_from_cappeff (false on empty/non-hex/over-long/signed input, never panics), unreadable /proc/self/status degrades the flag to false + warning, non-root always warns, root-without-cap warns; probe() never fails/exits; 4/4 daemon tests (bit-21 set/clear fixtures, malformed inputs, probe flag/warning consistency), workspace 195/195, clippy -D warnings clean, build green; fixture note: the brief's illustrative "0000002000000000" is bit 37, so tests use the true bit-21 hex "0000000000200000" and treat the brief's string as a false control.
AHEAD: P3-17 (main) must call ramsleuth_daemon::{probe, PrivilegeReport} — probe() -> PrivilegeReport{is_root, has_cap_sys_rawio, warnings}, eprint warnings to stderr, keep serving; P3-13 uses libc (chown/getgrnam) directly, not the caps API.
- [DONE] ID: P3-13 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-13
DECISION: built socket.rs — synchronous setup_listener(socket_path: &Path) -> Result<tokio::net::UnixListener, SocketSetupError{DirCreate, AlreadyRunning, StaleRemoveFailed, Bind, Chmod}>: create_dir_all on the parent (failure -> DirCreate with the `--socket /tmp/ramsleuth.sock` hint, no silent fallback), pre-existing file probed via UnixStream::connect (live -> AlreadyRunning, dead -> stale remove + rebind, failure -> StaleRemoveFailed), bind, chmod 0660 (hard Chmod error), best-effort chown ramsleuth->wheel via libc getgrnam/chown (warning only, deliberately no chown error arm), then set_nonblocking(true) + from_std; io::Error is not Clone/PartialEq so both traits are hand-implemented (variant + kind + message; Debug derived); lib.rs re-exports setup_listener + SocketSetupError; 4 new #[tokio::test]s in a per-pid temp dir with per-test cleanup (fresh bind + 0660 mode + live listener, stale rebind, live -> AlreadyRunning, uncreatable parent -> DirCreate hint); daemon 8/8, workspace 199/199, clippy -D warnings clean, build green.
AHEAD: P3-17 must call ramsleuth_daemon::setup_listener(&Path) from inside a tokio runtime context (tokio 1.53 from_std registers with the runtime IO driver and panics without one; the nonblocking requirement is handled internally), with socket_path = --socket (default ramsleuth_protocol::DEFAULT_SOCKET_PATH), before the P3-16 accept loop; the only hard errors are DirCreate/AlreadyRunning/StaleRemoveFailed/Bind/Chmod — chown never fails startup.
- [DONE] ID: P3-14 | STATUS: SUCCESS | BRANCH: branch/chunk-P3-14
DECISION: built cache.rs — TelemetryCache{collector: Box<dyn Fn() -> SystemMemoryTelemetry + Send + Sync>, ttl, last: Option<SystemMemoryTelemetry>, last_at: Option<Instant>} with an INJECTABLE collector: new(impl Fn() -> SystemMemoryTelemetry + Send + Sync + 'static, Duration), get(&mut self) -> SystemMemoryTelemetry (fresh -> clone of the cached snapshot, zero collector calls; stale/cold -> collect, store + Instant::now(), return by value; never panics by itself — a panicking collector is the collector's fault, the real collect() is no-panic by the Phase 2 contract), ttl() accessor; lib.rs gains `pub mod cache` + crate-root `pub use cache::TelemetryCache` (re-export style, doc module list already anticipated it); 4 unit tests with a mock counting collector (Arc<AtomicUsize> + all-Na SystemMemoryTelemetry via the telemetry public API): (a) first get = 1 call + value, (b) second get within TTL = still 1 call + equal clone, (c) 1 ms TTL + 20 ms sleep = 2 calls, (d) field-for-field mock equality + ttl accessor; daemon 12/12, workspace clippy -D warnings clean, build green; MSRV 1.75-safe (no post-1.75 APIs).
AHEAD: P3-16 (rpc) must call TelemetryCache::new(collector, ttl) + cache.get(&mut self) -> SystemMemoryTelemetry (clone within TTL) + cache.ttl() — the briefing's &mut self API (not the plan's RwLock sketch); P3-17 (main) constructs it with ramsleuth_telemetry::collect + the --max-age default (5 s).
